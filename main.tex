\documentclass[a4paper]{article}

\usepackage[a4paper, fancysections, titlepage]{polytechnique}
\usepackage[french]{babel}
\usepackage[T1]{fontenc}
\usepackage[hidelinks]{hyperref}
\usepackage{amsmath, amssymb}
\usepackage{braket}
\usepackage{tikz}
\usepackage{quantikz}
\usepackage{float}
\usetikzlibrary{positioning, arrows.meta, babel}

\author{Hannah Faucheu, Guillaume Henrotte, Achim Kobena Dongo, Tancrède Tonini, Kristen Boitier, Stanislas Mischler}
\date{Avril 2026}
\title{PSC INF : RAPPORT FINAL}
\subtitle{Machine Learning Quantique dans la Détection de Fraude Bancaire}
\logo{typographix}

\begin{document}
\maketitle
\tableofcontents

\section{Introduction}

La détection de fraude bancaire est aujourd'hui l'un des enjeux opérationnels majeurs des grandes institutions financières. Le volume toujours croissant des transactions, la diversification des canaux de paiement et l'adaptation permanente des comportements frauduleux rendent la tâche particulièrement difficile pour les approches classiques, qui doivent composer avec des jeux de données massifs, déséquilibrés et en constante évolution. Dans ce contexte, l'informatique quantique, et plus particulièrement le \textit{Quantum Machine Learning} (QML), apparaît comme une voie prometteuse pour dépasser certaines limites théoriques des algorithmes classiques.

Ce rapport final s'inscrit dans la continuité de nos travaux menés en collaboration avec BNP Paribas dans le cadre du PSC INF. Il présente la synthèse de notre démarche, depuis la définition du problème jusqu'à l'assemblage d'une pipeline hybride classique/quantique de détection de fraude, en passant par l'étude des briques algorithmiques individuelles — clustering, réseaux de neurones, détection d'anomalies — et par leur confrontation à des données réelles fournies par la banque.

\subsection{Motivation générale}

La détection de fraude constitue un enjeu à la fois économique, réglementaire et technique pour les institutions bancaires. Chaque transaction frauduleuse non détectée se traduit par une perte financière directe, une atteinte à la confiance des clients et une exposition accrue aux contraintes de conformité. À l'inverse, un taux trop élevé de faux positifs dégrade l'expérience utilisateur et mobilise inutilement les équipes d'analyse. Les méthodes classiques de \textit{machine learning}, bien que matures, atteignent progressivement leurs limites face à la croissance du volume et de la complexité des données financières, ainsi qu'à la nature intrinsèquement mouvante de la fraude.

Les algorithmes quantiques promettent, du moins sur le plan théorique, des gains significatifs en complexité et en expressivité par rapport à leurs équivalents classiques, notamment pour des tâches de clustering, de détection d'anomalies ou de recherche dans des espaces de grande dimension. Cette promesse reste toutefois largement conditionnée par la maturité du matériel actuel — processeurs NISQ bruités, nombre limité de qubits, absence de QRAM opérationnelle — et par la capacité à concevoir des algorithmes véritablement adaptés aux contraintes du domaine bancaire.

Notre projet vise précisément à explorer ce compromis entre potentiel théorique et faisabilité pratique. En collaboration avec BNP Paribas, nous avons cherché à concevoir et à implémenter une pipeline de détection de fraude intégrant des briques quantiques à des composantes classiques éprouvées, de manière à pouvoir mesurer rigoureusement l'apport de chaque approche. L'objectif final est double : d'une part, fournir à la BNP un proof of concept d'algorithme hybride capable d'analyser des transactions passées pour signaler les fraudes éventuelles sur de nouvelles transactions ; d'autre part, identifier les briques pour lesquelles l'approche quantique offre, dès aujourd'hui ou à moyen terme, un avantage tangible sur ses contreparties classiques.

\subsection{État de l'art}

La détection de fraude est un problème classique de classification supervisée fortement déséquilibrée, pour lequel la littérature classique est abondante (régressions logistiques, gradient boosting, forêts aléatoires, auto-encodeurs, réseaux de neurones profonds). Notre objectif n'est pas de rivaliser frontalement avec ces méthodes, mais d'examiner dans quelle mesure des algorithmes quantiques, ou hybrides classique/quantique, peuvent apporter un gain \emph{théorique} (complexité, expressivité) ou \emph{pratique} (qualité de détection) sur ce type de données. Nous distinguons quatre familles de travaux qui structurent notre état de l'art : les approches classiques de référence, le clustering quantique, la détection d'anomalies quantique, et les classifieurs QML supervisés.

\subsubsection{Approches classiques de référence}

Les méthodes classiques déployées en production reposent essentiellement sur des modèles supervisés entraînés sur des historiques étiquetés. Le gradient boosting (XGBoost, LightGBM) et les forêts aléatoires constituent aujourd'hui les \emph{baselines} usuelles, car ils gèrent bien les variables hétérogènes et offrent une bonne robustesse au bruit. Pour les jeux de données fortement déséquilibrés comme ceux de la fraude (où la classe positive représente typiquement moins de $0{,}2\%$ des transactions), l'emploi de techniques de rééchantillonnage telles que SMOTE \cite{chawla2002smote} et de prétraitements par ACP est devenu standard. En parallèle, les méthodes non supervisées — Isolation Forest, Local Outlier Factor, auto-encodeurs — sont utilisées soit pour pallier l'absence d'étiquettes fiables, soit pour construire des scores d'anomalie utilisés en amont d'un classifieur supervisé. Ces approches constituent le point de comparaison incontournable pour tout algorithme quantique prétendant améliorer la détection de fraude.

\subsubsection{Clustering quantique}

Le clustering quantique a émergé avec la popularisation du \emph{Quantum Machine Learning} (QML) \cite{qml_survey}. Deux grandes familles se distinguent. La première est celle des algorithmes \emph{théoriquement optimaux} supposant l'existence d'une QRAM : Kerenidis et al. proposent un algorithme de $q$-means \cite{kerenidis_qmeans} dont la complexité est polylogarithmique en la taille du jeu de données, ainsi qu'un clustering spectral quantique \cite{kerenidis_spectral} exploitant l'estimation de phase pour diagonaliser efficacement la matrice laplacienne. Ces résultats sont spectaculaires sur le plan asymptotique, mais reposent sur des hypothèses d'accès aux données encore débattues, la réalisation physique d'une QRAM efficace restant un problème ouvert.

La seconde famille, \emph{variationnelle}, est spécifiquement conçue pour les ordinateurs bruités de type NISQ (\emph{Noisy Intermediate-Scale Quantum}). Bermejo et Orús \cite{bermejo_orus} proposent ainsi un clustering variationnel où un circuit paramétré $U(\theta)$ est optimisé par une boucle classique pour minimiser une fonction de coût reflétant la séparation inter-clusters. Poggiali et al. \cite{poggiali_kmeans} décrivent une variante hybride de $k$-means dans laquelle le calcul des distances est délégué à un sous-circuit quantique. Ces approches renoncent à la garantie de complexité polylogarithmique, mais sont implémentables sur le matériel actuel.

\subsubsection{Détection d'anomalies quantique}

La détection d'anomalies quantique est un domaine plus jeune et plus fragile : plusieurs articles publiés entre 2019 et 2023 contiennent des erreurs importantes, comme l'illustre Guo et al. \cite{guo_anomaly}, qui réfutent l'algorithme de Liang et al. \cite{liang_anomaly} et en proposent une version corrigée fondée sur des rotations contrôlées. La démarche générale reprend le schéma classique en deux temps : (i) apprentissage d'une représentation du comportement « normal » par clustering, et (ii) identification des outliers par une mesure de distance ou de densité. Les variantes quantiques diffèrent essentiellement par l'étape (ii), où l'amplitude d'un état produit ou le résultat d'une estimation d'amplitude fournissent un score d'anomalie. Là encore, la quasi-totalité des publications suppose un accès QRAM, ce qui limite leur portée expérimentale immédiate.

Une approche récente particulièrement adaptée aux contraintes NISQ est celle des \emph{Quantum Autoencoders} (QAE), dont FiD-QAE \cite{fidqae2025} constitue une instance directement appliquée à la fraude bancaire. Le principe est simple et élégant : on encode chaque transaction $\vec{x}\in\mathbb{R}^N$ par \emph{amplitude encoding} dans $n = \lceil\log_2 N\rceil$ qubits, puis un encodeur paramétré $U(\theta)$ partitionne le registre en un sous-espace \emph{latent} $A$ (de dimension $n_A$) et un sous-espace \emph{trash} $B$ (de dimension $n_B$). On entraîne ensuite $\theta$ \emph{exclusivement sur les transactions légitimes} pour que le sous-espace $B$ soit comprimé sur un état de référence fixe $\ket{0}^{\otimes n_B}$, ce qui se mesure par la fidélité $F(\theta) = \langle 0|\rho_B(\theta)|0\rangle$ via un SWAP test. La fonction de coût minimisée est alors $\mathcal{L}(\theta) = 1 - \mathbb{E}_{\vec{x}}[F(\theta;\vec{x})]$. À l'inférence, une transaction frauduleuse — non vue à l'entraînement — produit une fidélité significativement plus basse, et un seuil $\tau$ permet de la signaler. L'élégance de cette construction réside dans son traitement implicite du déséquilibre de classes : on n'a jamais besoin d'exemples de fraude pour entraîner le modèle. Les auteurs rapportent une exactitude supérieure à $0{,}92$ et une précision supérieure à $0{,}80$ avec seulement quatre qubits, ce qui en fait un candidat sérieux pour notre brique de détection d'anomalies non supervisée.

\subsubsection{Classifieurs QML supervisés}

Le troisième pan de la littérature concerne les classifieurs supervisés quantiques, principalement les \emph{Variational Quantum Classifiers} (VQC) et les \emph{Quantum Neural Networks} (QNN). Un VQC encode une donnée classique $\vec{x}\in\mathbb{R}^d$ dans un état quantique $\ket{\psi(\vec{x})} = U_{\varphi(\vec{x})}\ket{0}^{\otimes n}$ via un \emph{feature map} $\varphi$, applique ensuite un ansatz paramétré $U(\theta)$, puis mesure une observable dont la valeur espérée définit un score de classification. Les paramètres $\theta$ sont optimisés classiquement (COBYLA, SPSA, Adam) pour minimiser une perte, typiquement la cross-entropie. L'introduction des \emph{feature maps} de Pauli et en particulier du \emph{ZZ feature map} par Havlíček et al. \cite{havlicek2019} a marqué un jalon en établissant un cadre reproductible pour exploiter les corrélations d'ordre deux entre variables dans l'espace de Hilbert.

Plusieurs travaux récents enrichissent significativement ce paysage et nourrissent directement nos choix de conception. El Alami et al. \cite{elalami2026} fournissent une analyse comparative récente et particulièrement pertinente pour notre projet. Ils évaluent trois architectures QML — VQC, \emph{Sampler QNN} (SQNN, qui extrait des échantillons de la distribution de mesure pour alimenter un optimiseur classique) et \emph{Estimator QNN} (EQNN, hybride à base d'estimation d'observables suivie d'un réseau de neurones classique) — en faisant varier trois feature maps (Pauli, ZZ, Z) et quatre ansätze (Real Amplitudes, Efficient SU2, Two Local, Pauli Two Design). Leurs expériences sont menées sur le jeu public \emph{European credit card} (celui fourni dans \texttt{Data/creditcard.csv}) et sur le simulateur BankSim, après rééquilibrage \emph{Random Under Sampling} à 50/50. Ils recourent à une analyse de variance (ANOVA) pour quantifier l'effet individuel et croisé des composants, ce qui constitue une contribution méthodologique utile : elle permet d'identifier les combinaisons feature map/ansatz statistiquement significatives, et de dégager des configurations dont l'exactitude dépasse $0{,}98$ de F1-score sur leurs jeux de test. Ce travail nous servira de référence explicite dans la section \ref{sec:clustering}, à la fois comme source d'inspiration méthodologique (le protocole d'évaluation systématique) et comme témoin des performances atteignables par des classifieurs QML purement supervisés — complémentaires de notre approche, qui privilégie la détection d'anomalies et le clustering non supervisé.

Là où El Alami et al. fixent manuellement les paires \emph{feature map}/\emph{ansatz} à comparer, Innan et al. proposent avec \emph{CircuitHunt} \cite{circuithunt2025} une démarche radicalement différente : ils \emph{automatisent} la recherche de circuits adaptés à la détection de fraude. Le principe repose sur un dataset pré-construit de circuits quantiques (KetGPT) couvrant des largeurs de 3 à 10 qubits, sur lequel on applique trois filtres successifs : un budget en qubits, un nombre maximal de paramètres entraînables (typiquement $P_{\max} = 30$), et une vérification de validité numérique par simulation à paramètres nuls. Chaque circuit candidat est ensuite intégré dans une architecture hybride uniforme — pré-réseau classique, encodage du vecteur d'entrée dans le circuit candidat, connexion résiduelle apprenable, post-réseau classique — et entraîné brièvement (5 époques) ; le meilleur macro-F1 sur validation détermine le circuit retenu, qui est alors ré-entraîné plus longuement. Sur le jeu \emph{Credit Card Fraud} (rééquilibré par SMOTE), le circuit sélectionné atteint un macro-F1 de $0{,}97$ et une AUC de $0{,}9946$, surpassant plusieurs architectures QML conçues à la main (EstimatorQNN, QGNN, SQNN, QFDNN). Cette méthodologie nous est doublement précieuse : d'une part comme \emph{baseline} très exigeante à dépasser, d'autre part comme inspiration pour notre propre démarche d'évaluation systématique des briques quantiques.

Enfin, dans une direction orthogonale, Ubale et al. \cite{qlstm2025} explorent la composante \emph{temporelle} des transactions en proposant un \emph{Quantum LSTM} hybride. Leur architecture chaîne (i) un LSTM classique extrayant des dépendances temporelles d'une fenêtre glissante de transactions, (ii) une couche pleinement connectée projetant l'état caché $h_T$ vers $n_{\text{qubits}}$ dimensions, (iii) un VQC composé d'un \emph{AngleEmbedding} ($R_y(x_i)\ket{0}$ par qubit) suivi de couches \emph{StronglyEntanglingLayers} paramétrées, (iv) une mesure dans la base de Pauli-$Z$ produisant un vecteur de features quantiques, et (v) une couche dense finale de classification. Tout est entraîné de bout en bout par rétropropagation, les gradients du circuit étant calculés par la \emph{parameter-shift rule}. Sur des données synthétiques de $50\,000$ transactions enrichies par des features géospatiales (distance haversine entre client et marchand) et temporelles, leur QLSTM dépasse une LSTM classique sur précision, rappel et F1, tout en restant entraînable sur CPU en environ une minute par époque. Cet article suggère qu'une partie de l'avantage potentiel du quantique en détection de fraude pourrait précisément résider dans le traitement de données séquentielles, perspective que nous garderons en tête si les jeux BNP comportent une structure temporelle exploitable.

\section{Présentation du projet}

\subsection{Méthode générale}

Notre démarche reprend et affine le squelette adopté par le groupe 2024-25 : une pipeline de détection de fraude articulée autour d'un bloc de \emph{clustering} — pour apprendre une représentation du comportement normal — et d'un bloc de \emph{détection d'anomalies} — pour signaler les transactions s'en écartant —, le tout assemblé dans une architecture modulaire dite \textit{plug \& play}. Notre contribution principale sur ce squelette est d'en pousser la décomposition : là où nos prédécesseurs traitaient le clustering et la détection d'anomalies comme deux étapes relativement couplées, nous cherchons à isoler plus finement les sous-étapes (préparation des données, extraction de features, apprentissage du modèle de normalité, calcul du score d'anomalie, seuillage de décision) afin d'autoriser une hybridation plus souple entre composantes classiques et quantiques. Chaque sous-étape respecte une interface commune et peut être remplacée par sa contrepartie alternative sans modifier le reste de la pipeline, ce qui nous permet de composer et de comparer des configurations entièrement classiques, entièrement quantiques, ou arbitrairement mixtes.

Les briques quantiques effectivement implémentées dans ce rapport sont concentrées autour du \emph{clustering quantique variationnel}, qui constitue le cœur algorithmique de notre contribution. D'autres pistes quantiques — en particulier au niveau du préprocessing (réduction de dimension, encodage, variantes variationnelles de détection d'anomalies) — ont été explorées de manière plus prospective et font l'objet de discussions qualitatives plutôt que d'un benchmark exhaustif. Les architectures QML supervisées évoquées dans l'état de l'art (VQC, QAE, QLSTM) servent quant à elles de points de comparaison bibliographiques et n'ont pas été réimplémentées dans le cadre de ce projet.

Notre travail s'est déroulé en trois phases. Dans un premier temps, l'ensemble du groupe s'est consacré à une étude collective de la littérature QML et à la prise en main des outils d'informatique quantique (Qiskit, simulateurs, primitives variationnelles). Dans un deuxième temps, les différentes briques ont été développées en parallèle, chacune faisant l'objet d'un traitement théorique et expérimental autonome. Le dernier mois a enfin été consacré à l'intégration de ces briques dans l'architecture modulaire décrite ci-dessus, et à la mise en place de benchmarks comparatifs classiques / quantiques sur un jeu de données commun.

À ce stade du projet, nous ne visons pas d'exécution sur processeur quantique réel : toutes nos expériences sont menées sur \texttt{AerSimulator}, ce qui nous autorise des tailles d'instance et des budgets de \textit{shots} supérieurs à ceux accessibles via les services cloud d'IBM, au prix de l'absence de bruit matériel. Nous ne faisons par ailleurs pas d'hypothèses simplificatrices fortes sur la distribution des données : contrairement au proof of concept de 2024-25, qui supposait des clusters gaussiens dans $\mathbb{R}^d$, notre pipeline est conçue pour s'accommoder de features hétérogènes et de distributions non paramétriques. Les limites matérielles et leurs conséquences sur la portée de nos conclusions sont discutées en section \ref{sec:clustering} et dans le chapitre consacré aux difficultés et critiques.

\subsection{Données utilisées}

Nous avons travaillé sur deux jeux de données complémentaires. Le premier, fourni par BNP Paribas, est le jeu cible de notre étude ; sa particularité est de comporter des variables catégorielles à forte cardinalité — identifiants de traders, localisations — qui ne se prêtent pas directement aux algorithmes géométriques sur lesquels reposent la plupart de nos briques de clustering et de détection d'anomalies. Nous consacrons une section dédiée (\ref{sec:bnp}) aux pistes explorées pour transformer ces features qualitatives en représentations numériques exploitables. Le second jeu, utilisé pour le benchmarking systématique de nos briques, est un jeu public de transactions Revolut publié sur Kaggle \cite{revolut_kaggle} : directement étiqueté en classes fraude / non-fraude, il fournit une vérité terrain nette qui nous permet de comparer rigoureusement les variantes classiques et quantiques de chaque brique, et d'isoler ainsi l'apport algorithmique indépendamment des difficultés de préparation propres aux données BNP.

\section{Données de la BNP}\label{sec:bnp}


\section{Techniques de preprocessing}

Le but du machine learning est de faire de l'apprentissage à partir de notre jeu de données. Cependant, une étape clé en amont de l'apprentissage est de modifier ce jeu de données pour le rendre plus exploitable, plus performant, plus apte à révéler de l'information. Ainsi nous présentons plusieurs méthodes dites de Preprocessing pour optimiser notre jeu de données : SMOTE, Tomek Links, PCA et RFE. Voici en quoi ils consistent et quel est leur intérêt : 
    
\begin{itemize}
\item SMOTE (Synthetic Minority Over-sampling TEchnique) : concerne l'apprentissage supervisé, c'est-à-dire qu'il nécessite des données labellisées (fraude ou non). L'idée est que, dans un cas qui arrive souvent où dans nos labels il y a peu de données frauduleuses, alors l'apprentissage est peu efficace car il compare souvent des données non frauduleuses entre elles et les cas de fraude sont donc rares, donc plus enrichissants. Ainsi le but est de rééquilibrer les proportions artificiellement. Pour la détection de fraude, on a souvent moins de 0.1 \% des données qui sont frauduleuses. L'idée est de plutôt s'approcher d'un ratio 99 \% - 1\% ou 95 \% - 5\%. Pour cela, on prend une paire de données frauduleuses et on ajoute à notre base de données une donnée frauduleuse qui se trouve à leur milieu dans le plan. On peut également choisir un barycentre différent ou le prendre de manière aléatoire. L'idée étant que notre nouveau point est proche de données frauduleuses, il est donc très suspect d'être frauduleux.

\item Tomek Links : concerne également l'apprentissage supervisé mais agit à l'inverse de SMOTE pour supprimer quelques points du plan pour optimiser l'apprentissage. Dans un cas comme le nôtre, où il y a une dissymétrie brutale entre le nombre de labels fraude et non fraude, on cherche à préserver au mieux nos données les plus intéressantes qui sont celles ayant le label fraude. Ainsi on élimine du plan les points non fraude qui sont trop proches des points fraude. On dit qu'une paire de points du plan forme un Tomek Link si chacun est le plus proche voisin de l'autre. Si on a une paire fraude-non fraude qui forme un Tomek Link, on supprime le point non fraude. La surreprésentation des points non fraude fait que la perte n'est pas significative tandis qu'on protège l'information contenue dans les labels fraude. On peut ensuite tout à fait itérer ce procédé.

\item PCA (Principal Component Analysis) : cet algorithme réduit le nombre de paramètres, c'est-à-dire la dimension du plan, et bascule dans un nouveau système de coordonnées pour optimiser l'accessibilité à l'information de la base de données. La trentaine de paramètres initiaux est superflue. On ne veut garder que les éléments les plus intéressants pour notre apprentissage et on dispose d'un critère simple de sélection. On calcule la matrice de covariance de nos paramètres. Puis on sélectionne le sous-espace propre associé aux plus grandes valeurs propres (plus grandes variances, donc avec le moins de redondance), ce qui est immédiat à faire après diagonalisation. Les vecteurs propres associés que nous prenons pour nouveaux paramètres sont orthonormés, ce qui garantit l'indépendance et la singularité des informations qu'on peut y exploiter.

\item RFE (Recursive Feature Elimination) : cet algorithme tout comme le précédent vise à réduire le nombre de paramètres. On applique un algorithme de machine learning sur notre jeu de données pour faire notre apprentissage (par exemple l'arbre de décision) puis on sélectionne le paramètre ayant un pouvoir de décision le plus faible, c'est-à-dire celui qui est le moins retenu quand on décide de la nature (fraude ou non) d'un nouveau point. Ce paramètre est ensuite éliminé et on recommence notre apprentissage de zéro. L'idée est d'itérer ce procédé pour finir avec un nombre restreint de paramètres (environ une dizaine).
\end{itemize}

\textbf{Ordre de la pipeline preprocessing} : l'ordre dans lequel on applique les algorithmes a son importance, ils ont été présentés dans l'ordre de la pipeline, voici pourquoi :

\begin{itemize}
\item SMOTE puis Tomek Links. Le SMOTE crée des données synthétiques entre les points existants. Le problème, c'est qu'il crée souvent des points bruités qui empiètent sur la classe majoritaire. En passant les Tomek Links après, on supprime les points qui sont trop proches les uns des autres entre les deux classes. Cela nettoie la frontière de décision que le SMOTE a rendue floue.

\item PCA ensuite. La PCA cherche à maximiser la variance. Si on fait la PCA avant de rééquilibrer les classes, elle sera totalement biaisée par la classe majoritaire. En faisant la PCA après le rééquilibrage et le nettoyage, on s'assure que les composantes principales représentent bien la structure des deux classes de manière équitable.

\item RFE après PCA. RFE est très coûteux car il nécessite d'entraîner l'algorithme à chaque fois. Ainsi on l'utilise pour affiner. Le seul bémol d'utiliser RFE après PCA, c'est qu'on perd l'interprétation heuristique qu'on peut faire sur l'importance d'un paramètre : l'œil humain ne peut plus lire des explications "naturelles".
\end{itemize}

Bien que la nécessité de faire du Preprocessing n'est plus à démontrer, il reste encore plusieurs indéterminations. Quels algorithmes faut-il mettre dans la pipeline ? Faut-il tous les prendre ou seulement certains parmi les 4 ? Ensuite, pour chaque algorithme gardé, il faut décider à quelle intensité on l'utilise en choisissant les paramètres comme le nombre de données frauduleuses à rajouter via SMOTE par exemple. 

Enfin il est également possible de rendre ces algorithmes quantiques (par exemple QSMOTE \cite{SMOTE2025}), c'est-à-dire agissant sur les vecteurs dans l'espace des états. Ou bien nous les utilisons pour optimiser notre base de données avant l'encodage quantique des vecteurs, ou bien nous les utilisons après l'encodage quantique, sur les vecteurs de notre FeatureMap. Le prétraitement post-mapping permet d'opérer directement sur la structure topologique de l'Espace de Hilbert, facilitant l'extraction de corrélations quantiques complexes qui resteraient cachées dans l'espace euclidien originel. 

\section{Algorithmes non supervisés}\label{sec:clustering}

\subsection{Clustering classique}

\subsection{Clustering quantique variationnel}
\label{subsec:vqc-clustering}

Après avoir passé en revue les principales techniques de clustering quantique, nous avons retenu l'approche \emph{variationnelle} (Variational Quantum Clustering, VQC) car c'est aujourd'hui la plus employée. Nous en reprenons directement la formulation proposée par Bermejo et Orús \cite{bermejo_orus}.

\subsubsection*{Encodage et états de référence}

On considère un jeu de $N$ points $\mathcal{X} = \{\vec{x}_1,\dots,\vec{x}_N\} \subset \mathbb{R}^d$ à partitionner en $k$ clusters. On associe à chaque point $\vec{x}_i$ un vecteur de paramètres $\vec{\theta}_i \in [0, 2\pi)^{n}$, où $n$ est le nombre de qubits dédiés à l'encodage. Dans sa forme la plus simple, l'encodage consiste en une séquence de rotations $R_Y(\theta_{i,j})$ appliquées aux $n$ qubits initialement dans l'état $\ket{0}^{\otimes n}$ :
\[
\ket{\psi(\vec{\theta}_i)} \;=\; \bigotimes_{j=1}^{n} R_Y(\theta_{i,j})\ket{0}
\;=\; \bigotimes_{j=1}^{n}\!\left(\cos\tfrac{\theta_{i,j}}{2}\ket{0} + \sin\tfrac{\theta_{i,j}}{2}\ket{1}\right).
\]
Ce schéma d'\emph{angle encoding} est le plus léger en profondeur, donc le mieux adapté aux contraintes NISQ \cite{abbou2025vqc}. Des encodages plus expressifs — \emph{ZZ feature map}, \emph{amplitude encoding}, ou ansatz entanglant paramétré par couches $U_{\text{ent}}(\vec{\theta}_i)$ — peuvent s'y substituer sans modifier le reste de l'algorithme.

À chaque cluster $c \in \{1,\dots,k\}$ est associé un \emph{état de référence} $\ket{\phi_c} \in (\mathbb{C}^2)^{\otimes n}$, tiré aléatoirement au début de l'exécution et \emph{gelé} pendant tout l'apprentissage. Les paramètres entraînables sont donc uniquement les $\{\vec{\theta}_i\}_{i=1}^N$, qui encodent la position de chaque point dans l'espace de Hilbert relativement aux ancres fixes $\{\ket{\phi_c}\}_{c=1}^k$. On notera que les $\ket{\phi_c}$ ne forment pas nécessairement une base orthonormée : ils sont tirés indépendamment et peuvent avoir des recouvrements non nuls entre eux, ce qui est sans incidence sur la validité de l'algorithme.

\subsubsection*{Matrice de fidélité et SWAP test}

Le cœur du calcul est la \emph{matrice de fidélité} $\mathcal{F} \in \mathbb{R}^{N\times k}$, définie par
\[
\mathcal{F}_{ic} \;=\; \bigl|\!\braket{\psi(\vec{\theta}_i)\,|\,\phi_c}\!\bigr|^2.
\]
On interprète $\mathcal{F}_{ic}$ comme un score continu d'appartenance du point $i$ au cluster $c$ : il vaut $1$ si $\ket{\psi(\vec{\theta}_i)} = \ket{\phi_c}$ (à phase globale près), et $0$ si les deux états sont orthogonaux. Contrairement à un $k$-means classique, qui produit une assignation dure, la formulation variationnelle manipule ici une \emph{distribution} d'appartenance que l'on contraint ensuite à se concentrer sur un unique cluster via un terme de pénalité (cf.~ci-dessous).

L'estimation effective de $\mathcal{F}_{ic}$ sur matériel quantique repose sur le \emph{SWAP test}, qui extrait la fidélité entre deux états sans nécessiter de tomographie complète. On prépare un qubit ancillaire $A$ dans $\ket{+} = \tfrac{1}{\sqrt{2}}(\ket{0}+\ket{1})$, on applique des portes contrôlées \texttt{CSWAP} entre $A$ et chaque paire de qubits correspondants de $\ket{\psi(\vec{\theta}_i)}$ et $\ket{\phi_c}$, puis on ramène $A$ dans la base $Z$ par une seconde Hadamard et on le mesure. On montre alors que la probabilité d'observer $A = 0$ vaut
\[
\mathbb{P}(A = 0) \;=\; \tfrac{1}{2}\Bigl(1 + \bigl|\!\braket{\psi(\vec{\theta}_i)\,|\,\phi_c}\!\bigr|^2\Bigr),
\]
d'où l'estimateur sans biais $\widehat{\mathcal{F}}_{ic} = 2\hat{p}_0 - 1$, où $\hat{p}_0$ est la fréquence empirique observée sur $P$ répétitions. L'erreur quadratique de l'estimation décroît en $\mathcal{O}(1/P)$.

\begin{figure}[H]
\centering
\begin{quantikz}[column sep=0.6cm, row sep={0.8cm,between origins}]
\lstick{$\ket{0}$} & \gate{H} & \ctrl{1} & \gate{H} & \meter{} \\
\lstick{$\ket{\psi_i}$} & \qwbundle{n} & \swap{1} & & \\
\lstick{$\ket{\phi_c}$} & \qwbundle{n} & \targX{} & &
\end{quantikz}
\caption{Circuit du \emph{SWAP test} estimant la fidélité $|\!\braket{\psi_i|\phi_c}\!|^2$. La ligne du haut est le qubit ancillaire ; les lignes inférieures, annotées $n$, représentent les deux registres de $n$ qubits portant respectivement l'encodage du point $\ket{\psi_i} = \ket{\psi(\vec{\theta}_i)}$ et l'état de référence $\ket{\phi_c}$. La porte \texttt{CSWAP} échange ces deux registres qubit à qubit, conditionnée à l'ancilla. La mesure finale de l'ancilla fournit un estimateur non biaisé de la fidélité.}
\label{fig:swap-test}
\end{figure}

\subsubsection*{Fonction de coût}

Soit $D \in \mathbb{R}^{N \times N}$ la matrice des distances euclidiennes $D_{ij} = \|\vec{x}_i - \vec{x}_j\|_2$, calculée \emph{une fois pour toutes} en prétraitement classique. La fonction de coût variationnelle s'écrit comme une combinaison linéaire de deux termes :
\[
\mathcal{C}(\vec{\theta}) \;=\; \underbrace{\sum_{c=1}^{k}\sum_{i,j=1}^{N} D_{ij}\,\mathcal{F}_{ic}\,\mathcal{F}_{jc}}_{\mathcal{C}_{\text{cluster}}(\vec{\theta})}
\;+\;
\lambda \underbrace{\sum_{i=1}^{N}\Bigl(\sum_{c=1}^{k} \mathcal{F}_{ic} - 1\Bigr)^{\!2}}_{\mathcal{C}_{\text{pénalité}}(\vec{\theta})}.
\]
Le premier terme $\mathcal{C}_{\text{cluster}}$ pénalise l'appartenance simultanée de deux points \emph{géométriquement éloignés} à un même cluster : il est d'autant plus grand que des paires $(i,j)$ à $D_{ij}$ élevée se voient attribuer des masses de fidélité $\mathcal{F}_{ic}\mathcal{F}_{jc}$ non négligeables sur un cluster commun. Sa minimisation encourage donc la compacité géométrique des groupes.

Le second terme $\mathcal{C}_{\text{pénalité}}$ impose que chaque point totalise une masse de fidélité proche de $1$ sur l'ensemble des $k$ clusters, empêchant la solution dégénérée $\mathcal{F}_{ic} \to 0$ qui annulerait trivialement $\mathcal{C}_{\text{cluster}}$ sans produire de clustering. Il force ainsi, à la limite $\lambda \to \infty$, un comportement « one-hot » où chaque point se concentre sur un unique cluster.

Le scalaire $\lambda > 0$ règle l'équilibre entre ces deux contraintes et constitue l'unique hyperparamètre proprement variationnel du modèle. Un $\lambda$ trop faible autorise des assignations floues sans incitation à clusteriser nettement ; un $\lambda$ trop grand fait dominer la pénalité au point de masquer la géométrie sous-jacente. Les expériences du groupe précédent fixent $\lambda = 10$, valeur que nous retenons.

\subsubsection*{Algorithme complet}

L'algorithme complet assemble les ingrédients précédents en une boucle classique–quantique (figure~\ref{fig:vqc-loop}) :
\begin{enumerate}
    \item \textbf{Initialisation.} Normaliser les données, calculer une fois pour toutes la matrice $D$, tirer aléatoirement $\{\vec{\theta}_i\}_{i=1}^N \in [0,2\pi)^{n \cdot N}$ et $\{\ket{\phi_c}\}_{c=1}^k$.
    \item \textbf{Évaluation quantique.} Pour chaque couple $(i,c)$, préparer $\ket{\psi(\vec{\theta}_i)}$ et $\ket{\phi_c}$, exécuter un SWAP test, et assembler la matrice $\mathcal{F}(\vec{\theta})$.
    \item \textbf{Calcul classique du coût.} Évaluer $\mathcal{C}(\vec{\theta})$ à partir de $D$ et $\mathcal{F}$.
    \item \textbf{Mise à jour.} Utiliser un optimiseur classique (L-BFGS-B avec gradients numériques dans notre implémentation) pour mettre à jour $\vec{\theta}$, puis répéter les étapes 2–4 jusqu'à convergence.
    \item \textbf{Assignation finale.} Pour chaque point $i$, retourner le cluster $c_i = \arg\max_{c}\, \mathcal{F}_{ic}$.
\end{enumerate}

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    node distance=0.9cm and 1.2cm,
    box/.style={draw, rounded corners, align=center, minimum height=0.9cm, font=\small},
    qbox/.style={box, fill=blue!10},
    cbox/.style={box, fill=orange!15},
    arr/.style={-{Latex[length=2mm]}, thick}
]
\node[cbox] (data) {Données $\mathcal{X}$,\\matrice $D$};
\node[cbox, right=of data] (init) {Paramètres\\$\vec{\theta}$};
\node[qbox, right=of init] (circ) {Circuit\\$\ket{\psi(\vec{\theta}_i)}$};
\node[qbox, right=of circ] (swap) {SWAP\\test};
\node[cbox, below=of swap] (fid) {Matrice $\mathcal{F}$};
\node[cbox, left=of fid] (cost) {Coût\\$\mathcal{C}(\vec{\theta})$};
\node[cbox, left=of cost] (opt) {Optimiseur\\L-BFGS-B};

\draw[arr] (data) -- (init);
\draw[arr] (init) -- (circ);
\draw[arr] (circ) -- (swap);
\draw[arr] (swap) -- (fid);
\draw[arr] (fid) -- (cost);
\draw[arr] (cost) -- (opt);
\draw[arr] (opt.north) -- node[midway, left, font=\scriptsize] {$\vec{\theta} \leftarrow \vec{\theta} + \Delta\vec{\theta}$} (init.south);

\node[draw, dashed, fit=(circ)(swap), inner sep=4pt, label=above:{\footnotesize \emph{Monde quantique}}] {};
\end{tikzpicture}
\caption{Boucle d'apprentissage hybride du clustering variationnel quantique. Les blocs bleutés correspondent aux étapes exécutées sur simulateur/processeur quantique ; les blocs orange sont réalisés classiquement.}
\label{fig:vqc-loop}
\end{figure}

\subsubsection*{Choix du nombre de qubits et complexité}

Le nombre $n$ de qubits dédiés à l'encodage joue un rôle crucial, mis empiriquement en évidence dans le rapport 2024-25. Deux régimes sont à éviter. En régime de \emph{sous-paramétrisation} ($2^n < d$), la dimension de l'espace de Hilbert est trop faible pour distinguer les clusters et l'encodage projette des points distincts sur des états quasi confondus, induisant des fusions parasites. En régime de \emph{sur-paramétrisation} ($2^n \gg d$), on effectue beaucoup de calculs inutiles et l'optimisation devient moins efficace. En pratique, choisir $n \approx \lceil\log_2 d\rceil$ offre le meilleur compromis.

La complexité par itération de l'optimiseur se décompose en deux contributions. Côté quantique, il faut $\mathcal{O}(N k \cdot P)$ exécutions élémentaires du circuit SWAP test pour remplir la matrice de fidélité $\mathcal{F}$, où $N$ est le nombre de points, $k$ le nombre de clusters, et $P$ le nombre de répétitions utilisées pour estimer chaque fidélité. Côté classique, l'évaluation de la fonction de coût $\mathcal{C}(\vec{\theta})$ à partir de $\mathcal{F}$ et $D$ coûte $\mathcal{O}(N^2 k)$ opérations, en raison de la double somme sur les paires $(i,j)$ dans le terme $\mathcal{C}_{\text{cluster}}$.

\subsection{Implémentation}

\subsubsection*{Métrique d'évaluation : l'indice de Rand ajusté (ARI)}

Pour évaluer la qualité du clustering, nous utilisons l'\emph{indice de Rand ajusté} (ARI, \emph{Adjusted Rand Index}), qui mesure la similarité entre la partition prédite $\hat{\mathcal{P}} = \{\hat{C}_1,\dots,\hat{C}_k\}$ et la partition de référence $\mathcal{P}^{\star} = \{C_1^{\star},\dots,C_{k'}^{\star}\}$. En notant $n_{ij} = |\hat{C}_i \cap C_j^{\star}|$, $a_i = \sum_j n_{ij}$ et $b_j = \sum_i n_{ij}$,
\[
\mathrm{ARI} \;=\; \frac{\displaystyle\sum_{i,j}\binom{n_{ij}}{2} \;-\; \tfrac{1}{\binom{N}{2}}\sum_i \binom{a_i}{2}\sum_j \binom{b_j}{2}}{\tfrac{1}{2}\!\left(\sum_i \binom{a_i}{2} + \sum_j \binom{b_j}{2}\right) \;-\; \tfrac{1}{\binom{N}{2}}\sum_i \binom{a_i}{2}\sum_j \binom{b_j}{2}}.
\]
Le numérateur compte les paires de points regroupées à l'identique dans les deux partitions (terme $\sum_{i,j}\binom{n_{ij}}{2}$), auquel on retranche le nombre attendu de telles paires sous tirage aléatoire. Ainsi, $\mathrm{ARI} = 1$ lorsque les deux partitions coïncident parfaitement et $\mathrm{ARI} = 0$ lorsque la partition prédite n'est pas meilleure qu'un tirage au hasard.

\subsubsection*{Première expérience}

Nous avons implémenté l'algorithme VQC décrit plus haut en \emph{Qiskit}. Le jeu de données synthétique est constitué de trois \emph{blobs} gaussiens dans $\mathbb{R}^3$, chacun de cardinalité $20$ (soit $N = 60$ points à partitionner en $k = 3$ clusters). Nous avons ensuite exécuté la boucle hybride décrite précédemment pendant $30$ itérations de l'optimiseur, avec l'hyperparamètre $\lambda = 10$.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\linewidth]{.png/VQC_first_results.png}
\caption{Résultats de la première exécution du clustering variationnel quantique sur trois blobs gaussiens dans $\mathbb{R}^3$ ($N = 60$, $k = 3$, $\lambda = 10$, $30$ itérations).}
\label{fig:vqc-first-results}
\end{figure}

L'ARI obtenu sur cette exécution vaut $0{,}950$, ce qui signifie que la VQC a bien compris la répartition en clusters. Ce résultat confirme que la boucle variationnelle parvient à exploiter la géométrie des blobs malgré le faible nombre d'itérations, et valide notre implémentation.

\subsubsection*{Application au jeu de données de cartes bancaires}

Après avoir validé l'implémentation sur des données synthétiques, nous avons appliqué le clustering variationnel quantique au jeu de données de cartes bancaires de Kaggle. Nous en tirons un sous-ensemble de $N = 200$ transactions, dont $10$ fraudes avérées. L'algorithme est exécuté avec $n = 6$ qubits et l'hyperparamètre $\lambda = 10$. Afin de visualiser la partition obtenue dans un espace de dimension raisonnable, nous projetons les données sur les trois composantes principales issues d'une ACP, puis affichons l'affectation des points aux clusters (figure~\ref{fig:qcl-clusters}).

\begin{figure}[H]
\centering
\includegraphics[width=0.55\linewidth]{.png/Quantum_clustering_1.png}
\caption{Clustering quantique variationnel sur $200$ transactions ($10$ fraudes, $n = 6$ qubits, $\lambda = 10$), visualisé dans l'espace des trois composantes principales.}
\label{fig:qcl-clusters}
\end{figure}

Pour détecter les fraudes à partir de cette partition, nous exploitons l'incertitude d'affectation du VQC. Pour chaque transaction $i$, nous disposons des probabilités d'appartenance $F_{ic}$ à chacun des clusters $c$ et définissons le score de confiance $s_i = \max_c F_{ic}$. Nous fixons alors un seuil $\tau$ tel qu'une transaction vérifiant $s_i < \tau$ soit considérée comme mal affectée et étiquetée comme fraude. Le seuil optimal $\tau^\star$ est obtenu en itérant sur l'ensemble des valeurs admissibles et en retenant celui qui maximise le score $F_1$ sur les $10$ fraudes connues. La courbe d'évolution du $F_1$ en fonction du seuil est reportée sur la figure~\ref{fig:qcl-curve} et les résultats optimaux sont rassemblés dans le tableau~\ref{tab:qcl-results}.

\begin{figure}[H]
\centering
\includegraphics[width=0.55\linewidth]{".png/QVC Curve.png"}
\caption{Évolution du score $F_1$ du VQC en fonction du seuil de confiance $\tau$.}
\label{fig:qcl-curve}
\end{figure}

\begin{table}[H]
\centering
\begin{tabular}{lc}
\hline
Métrique & Valeur \\
\hline
Seuil optimal $\tau^\star$ & $0{,}0009$ \\
$F_1$ & $0{,}5833$ \\
Précision & $0{,}5000$ ($7$ TP / $14$ signalés) \\
Rappel & $0{,}7000$ ($7$ TP / $10$ fraudes) \\
Transactions signalées & $14 / 200$ \\
\hline
\end{tabular}
\caption{Résultats optimaux du clustering quantique variationnel sur le sous-ensemble de $200$ transactions.}
\label{tab:qcl-results}
\end{table}

\subsubsection*{Benchmark avec l'algorithme $k$-means}

Nous avons ensuite appliqué la même procédure au clustering classique des $k$-means sur le même sous-ensemble, afin de disposer d'un point de comparaison. La stratégie de détection diffère légèrement : plutôt que d'utiliser une probabilité d'affectation, nous exploitons la distance euclidienne $d_i$ de chaque transaction au centroïde de son cluster d'affectation. Une transaction est signalée comme fraude si $d_i > \tau$, et le seuil $\tau$ est à nouveau choisi pour maximiser le $F_1$. Les clusters obtenus sur les trois premières composantes principales sont présentés sur la figure~\ref{fig:kmeans-clusters}, la courbe d'évolution du $F_1$ sur la figure~\ref{fig:kmeans-curve} et les résultats optimaux dans le tableau~\ref{tab:kmeans-results}.

\begin{figure}[H]
\centering
\includegraphics[width=0.55\linewidth]{.png/k-means_points.png}
\caption{Clustering $k$-means sur les mêmes $200$ transactions, visualisé dans l'espace des trois composantes principales.}
\label{fig:kmeans-clusters}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.55\linewidth]{.png/k-means-curve.png}
\caption{Évolution du score $F_1$ du $k$-means en fonction du seuil de distance au centroïde.}
\label{fig:kmeans-curve}
\end{figure}

\begin{table}[H]
\centering
\begin{tabular}{lc}
\hline
Métrique & Valeur \\
\hline
Seuil optimal $\tau^\star$ & $6{,}4702$ \\
$F_1$ & $0{,}7200$ \\
Précision & $0{,}6000$ ($9$ TP / $15$ signalés) \\
Rappel & $0{,}9000$ ($9$ TP / $10$ fraudes) \\
Transactions signalées & $15 / 200$ \\
\hline
\end{tabular}
\caption{Résultats optimaux du $k$-means sur le même sous-ensemble de $200$ transactions.}
\label{tab:kmeans-results}
\end{table}

\paragraph{Interprétation.} De prime abord, le $k$-means demeure plus efficace que le clustering quantique variationnel, avec un $F_1$ supérieur ($0{,}72$ contre $0{,}58$) et des clusters visuellement plus nets. L'écart se creuse en particulier sur le \emph{rappel} ($0{,}90$ contre $0{,}70$), ce qui est d'autant plus significatif que cette métrique est la plus critique en détection de fraude : manquer une transaction frauduleuse (faux négatif) coûte bien plus cher à la banque qu'une alerte injustifiée. Une explication plausible de ce déficit réside dans la simplicité des transformations considérées dans notre circuit d'encodage : la \emph{feature map} utilisée ne produit pas un plongement suffisamment riche pour que les fraudes se distinguent clairement des transactions légitimes via les probabilités $F_{ic}$. Il apparaît donc nécessaire d'améliorer cette approche — par exemple en enrichissant la \emph{feature map} ou en repensant le critère de détection — ce que nous explorons dans le paragraphe suivant, avant de nous tourner, dans la section suivante, vers un algorithme quantique plus spécifiquement conçu pour la détection d'anomalies, le Q-OCSVM.

\subsubsection*{Clustering quantique variationnel amélioré}

Un examen critique de l'implémentation de base révèle trois limitations structurelles qui bornent ce que le modèle peut effectivement apprendre.

\begin{enumerate}
    \item \textbf{Absence d'intrication.} Le circuit d'encodage se réduit à un produit tensoriel de blocs $[\cos(\theta/2), \sin(\theta/2)]$ qubit par qubit (uniquement des portes $R_Y$, sans $\mathrm{CNOT}$ ni $\mathrm{CZ}$). L'ensemble des états atteignables est donc un tore de dimension $n_{\text{qubits}}$ plongé dans l'espace de Hilbert de dimension $2^{n_{\text{qubits}}}$ : nous restons confinés aux états produits classiques et nous n'exploitons aucune corrélation quantique.
    \item \textbf{Amplitudes exclusivement réelles.} Les portes utilisées n'engendrent que des amplitudes réelles ($\cos$ et $\sin$ d'angles réels), alors que les références cibles tirées uniformément dans $\mathbb{C}^{2^n}$ possèdent une partie imaginaire. Les fidélités $|\!\braket{\phi|\psi}\!|^2$ ne retiennent que la composante réelle et un grand nombre de références distinctes deviennent indiscernables sur cette variété.
    \item \textbf{Pénalité de normalisation mal posée.} La contrainte $\sum_c \mathcal{F}_{ic} \to 1$ n'est valide que si les $\{\ket{\phi_c}\}$ forment une base orthonormée complète ($k = d$). Or, avec $k = 3$ vecteurs tirés dans $d = 2^5 = 32$ dimensions, l'espérance de cette somme vaut $k/d$, et non $1$ ; la pénalité pousse l'optimiseur vers une cible inatteignable.
\end{enumerate}

Nous reconstruisons le modèle de façon à lever simultanément ces trois obstacles, tout en conservant la méthode de détection par balayage de seuil sur $s_i = \max_c \mathcal{F}_{ic}$ de la section précédente.

\paragraph{Rotations à un qubit.} Les portes $R_X(\theta)$, $R_Y(\theta)$ et $R_Z(\theta)$ désignent les rotations d'un qubit d'un angle $\theta$ autour des axes $x$, $y$ et $z$ de la sphère de Bloch, définies par $R_\sigma(\theta) = \exp(-i\,\theta\,\sigma/2)$ où $\sigma \in \{X, Y, Z\}$ est la matrice de Pauli correspondante. Matriciellement,
\[
R_Y(\theta) = \begin{pmatrix} \cos\tfrac{\theta}{2} & -\sin\tfrac{\theta}{2} \\ \sin\tfrac{\theta}{2} & \phantom{-}\cos\tfrac{\theta}{2} \end{pmatrix}, \quad R_Z(\theta) = \begin{pmatrix} e^{-i\theta/2} & 0 \\ 0 & e^{i\theta/2} \end{pmatrix}, \quad R_X(\theta) = \begin{pmatrix} \cos\tfrac{\theta}{2} & -i\sin\tfrac{\theta}{2} \\ -i\sin\tfrac{\theta}{2} & \phantom{-i}\cos\tfrac{\theta}{2} \end{pmatrix}.
\]
En particulier, $R_Y$ appliquée à $\ket{0}$ n'engendre que des amplitudes réelles, ce qui correspond à la limitation (2) identifiée plus haut et justifie l'introduction de $R_Z$ (qui ajoute des phases complexes) dans le bloc variationnel.

\paragraph{Modifications apportées.}
\begin{itemize}
    \item \emph{Feature map avec intrication.} Nous appliquons $R_Y(x)$ sur chaque qubit, puis un anneau de portes $\mathrm{CZ}$, puis $R_Y(2x)$ sur chaque qubit. À partir de $\ket{0}^{\otimes n}$, les caractéristiques (coordonnées ACP des transactions) produisent désormais un état véritablement intriqué, dont les amplitudes dépendent non linéairement des entrées.
    \item \emph{Bloc variationnel partagé.} Nous ajoutons $n_{\text{layers}}$ couches composées, pour chaque qubit, d'une séquence $R_Y$–$R_Z$–$R_Y$ suivie d'un anneau $\mathrm{CZ}$. Les angles entraînables $\theta$, de forme $(n_{\text{layers}}, n_{\text{qubits}}, 3)$, sont partagés entre tous les échantillons, ce qui ramène le nombre de paramètres à quelques dizaines (au lieu de $N\cdot n$) et rend la descente \emph{L-BFGS-B} à différences finies peu coûteuse.
    \item \emph{États de cluster libres.} Chaque $\ket{\phi_c}$ est un vecteur complexe plein de $\mathbb{C}^{2^{n_{\text{qubits}}}}$, renormalisé à l'usage. Les références ne sont plus contraintes à la sous-variété des états produits.
    \item \emph{Fonction de coût corrigée.}
    \[
    \mathcal{C}(\theta, \Phi) \;=\; \underbrace{\tfrac{1}{N}\sum_{i=1}^{N}\bigl(1 - \mathrm{smax}_\tau(\mathcal{F}_{i\cdot})\bigr)^2}_{\text{couverture}}
    \;+\; \tfrac{1}{2}\,\underbrace{\tfrac{1}{k^2}\,\bigl\|\Phi\Phi^\dagger - I_k\bigr\|_F^2}_{\text{séparation}}
    \]
    où $\mathrm{smax}_\tau$ est un substitut lisse de $\max_c \mathcal{F}_{ic}$. Le terme de \emph{couverture} pousse chaque échantillon à s'affecter franchement à un cluster ; le terme de \emph{séparation} remplace la pénalité mal posée $\sum_c \mathcal{F}_{ic} = 1$ par la contrainte que les états de cluster soient deux à deux orthonormés ($\Phi\Phi^\dagger \approx I_k$), condition sous laquelle les fidélités somment effectivement à $1$.
    \item \emph{Tête de détection inchangée.} Chaque point reçoit le score $1 - \max_c \mathcal{F}_{ic}$ ; on balaye ensuite le seuil de coupure et on retient celui qui maximise le $F_1$, exactement comme pour le VQC de base et le $k$-means.
\end{itemize}

Nous appliquons ce modèle amélioré au même sous-ensemble de $200$ transactions (dont $10$ fraudes), avec les mêmes hyperparamètres ($n = 6$ qubits, projection ACP identique). La figure~\ref{fig:qcl-improved-clusters} montre l'affectation des points aux clusters sur les trois composantes principales, la figure~\ref{fig:qcl-improved-curve} la courbe d'évolution du $F_1$ en fonction du seuil, et le tableau~\ref{tab:qcl-improved-results} récapitule les scores au seuil optimal.

\begin{figure}[H]
\centering
\includegraphics[width=0.55\linewidth]{".png/Improved QVC points.png"}
\caption{Clustering quantique variationnel \emph{amélioré} sur les mêmes $200$ transactions, visualisé dans l'espace des trois composantes principales.}
\label{fig:qcl-improved-clusters}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.55\linewidth]{".png/Improved QVC curve.png"}
\caption{Évolution du score $F_1$ du VQC amélioré en fonction du seuil de confiance $\tau$.}
\label{fig:qcl-improved-curve}
\end{figure}

\begin{table}[H]
\centering
\begin{tabular}{lc}
\hline
Métrique & Valeur \\
\hline
Seuil optimal $\tau^\star$ & $0{,}9853$ \\
$F_1$ & $0{,}6087$ \\
Précision & $0{,}5385$ ($7$ TP / $13$ signalés) \\
Rappel & $0{,}7000$ ($7$ TP / $10$ fraudes) \\
Transactions signalées & $13 / 200$ \\
\hline
\end{tabular}
\caption{Résultats optimaux du clustering quantique variationnel \emph{amélioré} sur le sous-ensemble de $200$ transactions.}
\label{tab:qcl-improved-results}
\end{table}

\paragraph{Bilan.} Cette version corrigée atteint $F_1 = 0{,}61$ (contre $0{,}58$ pour le VQC de base), avec un rappel identique ($0{,}70$) mais une précision légèrement améliorée ($0{,}538$ contre $0{,}500$). Le gain, réel mais modeste, confirme que l'intrication, les amplitudes complexes et la contrainte d'orthonormalité apportent une expressivité supplémentaire ; il reste toutefois insuffisant pour combler l'écart avec le $k$-means sur ce jeu, ce qui nous a amené à pousser l'approche encore plus loin dans une dernière version décrite ci-après.

\subsubsection*{Clustering quantique variationnel v3}

Enfin, nous présentons une dernière amélioration du clustering quantique, qui s'appuie sur la version précédente en lui ajoutant deux ingrédients : un terme de coût qui préserve la structure locale des données, et un entraînement robuste empêchant les fraudes de contaminer le signal d'apprentissage.

\paragraph{Changements par rapport à la version améliorée.}
\begin{itemize}
    \item \emph{Préservation des similarités.} Nous ajoutons au coût un terme $\mathcal{C}_{\text{sim}} = \frac{1}{|E|}\sum_{(i,j)\in E}\!\bigl(1 - |\!\braket{\psi_i|\psi_j}\!|^2\bigr)^2$, sommé sur les arêtes $E$ d'un graphe des $k_{\text{NN}} = 5$ plus proches voisins calculé sur les données pré-encodées. Ce terme force les points proches dans l'espace initial à le rester dans l'espace de Hilbert, ce qui injecte explicitement l'information de distance qui n'entrait jusqu'ici qu'implicitement via la continuité du plongement.
    \item \emph{Entraînement robuste par élagage.} Avant l'optimisation, nous appliquons un $k$-means classique sur les données pré-encodées et écartons les $5~\%$ de points les plus éloignés de leur centroïde. Ces points, supposés être des fraudes en puissance, sont exclus à la fois du terme de couverture et du graphe $E$. Sans cet élagage, la couverture inciterait l'optimiseur à aspirer les fraudes dans les clusters et ferait s'effondrer la capacité de détection ; avec lui, les ancres se calent sur la masse saine. Les points écartés sont en revanche toujours scorés à l'inférence.
    \item \emph{Tête de détection inchangée.} On conserve $s_i = 1 - \max_c \mathcal{F}_{ic}$ et le balayage du seuil maximisant le $F_1$.
\end{itemize}

Nous appliquons ce modèle au même sous-ensemble de $200$ transactions. Les résultats sont rassemblés sur la figure~\ref{fig:qcl-v3-plots} (affectations, étiquettes réelles et courbe $F_1$ vs seuil) et dans le tableau~\ref{tab:qcl-v3-results}.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\linewidth]{".png/QVC v3 plots.png"}
\caption{VQC v3 : affectation aux $k=3$ clusters (gauche), étiquettes réelles (centre), et évolution du $F_1$ en fonction du seuil de détection $\tau$ (droite). Les points sont projetés sur les trois premières composantes ACP.}
\label{fig:qcl-v3-plots}
\end{figure}

\begin{table}[H]
\centering
\begin{tabular}{lc}
\hline
Métrique & Valeur \\
\hline
Seuil optimal $\tau^\star$ & $0{,}9275$ \\
$F_1$ & $0{,}8000$ \\
Précision & $0{,}6667$ ($10$ TP / $15$ signalés) \\
Rappel & $1{,}0000$ ($10$ TP / $10$ fraudes) \\
Transactions signalées & $15 / 200$ \\
\hline
\end{tabular}
\caption{Résultats optimaux du clustering quantique variationnel \emph{v3} sur le sous-ensemble de $200$ transactions.}
\label{tab:qcl-v3-results}
\end{table}

\paragraph{Bilan.} Cette version atteint $F_1 = 0{,}80$, avec un rappel \emph{parfait} ($1{,}00$, soit $10/10$ fraudes détectées) pour $15$ transactions signalées sur $200$, et dépasse ainsi le $k$-means ($F_1 = 0{,}72$, rappel $0{,}90$). Ce gain est en grande partie imputable au \emph{trimming}, qui empêche les fraudes de contaminer l'entraînement, et à la préservation des similarités, qui donne au plongement une géométrie plus cohérente. Nous poursuivons maintenant avec l'examen du Q-OCSVM

\subsection{Rappels théoriques : SVM classique et astuce du noyau}
\label{subsec:svm-classique}

Avant d'aborder sa variante quantique, il est indispensable de poser le cadre mathématique du Support Vector Machine (SVM) classique, car l'approche quantique vient s'y greffer directement sans en altérer la logique d'optimisation. 

À l'origine, le SVM est un modèle de classification binaire cherchant à séparer linéairement des données réparties en deux classes \cite{cortes1995svm}. L'idée directrice n'est pas seulement de trouver un hyperplan séparateur d'équation $\vec{w} \cdot \vec{x} + b = 0$, mais de trouver celui qui maximise la marge, c'est-à-dire la distance entre l'hyperplan et les points les plus proches de chaque classe.

\subsubsection*{Le problème primal et la marge souple}

Mathématiquement, maximiser cette marge revient à minimiser la norme du vecteur normal à l'hyperplan, $\|\vec{w}\|$. Or, dans le cas de données réelles (et a fortiori dans la détection de fraude), les classes se chevauchent souvent à cause du bruit. Il est donc impossible de tracer une frontière parfaite. Pour y remédier, nous introduisons des variables réelles $\xi_i \geq 0$ qui autorisent certaines erreurs de classification moyennant une pénalité. 

Le problème d'optimisation, dit « primal », s'écrit alors :
\[
\min_{\vec{w}, b, \vec{\xi}} \;\frac{1}{2} \|\vec{w}\|^2 + C \sum_{i=1}^{N} \xi_i
\]
sous les contraintes $y_i(\vec{w} \cdot \vec{x}_i + b) \geq 1 - \xi_i$. Le paramètre de régularisation $C$ contrôle le compromis entre la largeur de la marge (généralisation) et la pénalisation des erreurs sur le jeu d'entraînement. $y_i \in \{-1, 1\}$ est le label du point $x_i$. $y_i(\vec{w} \cdot \vec{x}_i + b)$ est censé être supérieur ou égal à $1$, ainsi $\xi_i$ est la variable de pénalisation de cette contrainte qu'on se permet d'assouplir. 

\subsubsection*{Passage au problème dual}

La formulation primale dépend directement de la dimensionnalité des données. Si nous voulons projeter nos données dans un espace très complexe pour capturer des comportements non linéaires, la taille de $\vec{w}$ explosera, rendant le calcul intraitable. 

La solution canonique consiste à passer par la formulation duale de ce problème d'optimisation en introduisant les multiplicateurs de Lagrange $\alpha_i$. Après dérivation et annulation du gradient par rapport à $\vec{w}$ et $b$, le problème devient la maximisation d'une fonction quadratique :
\[
\max_{\vec{\alpha}} \; \sum_{i=1}^{N} \alpha_i - \frac{1}{2} \sum_{i=1}^{N} \sum_{j=1}^{N} \alpha_i \alpha_j y_i y_j (\vec{x}_i \cdot \vec{x}_j)
\]
sous les contraintes $0 \leq \alpha_i \leq C$ et $\sum_{i} \alpha_i y_i = 0$.

Ce problème présente deux propriétés fondamentales pour la suite de notre architecture :
\begin{enumerate}
    \item \textbf{Parcimonie des solutions :} La majorité des multiplicateurs $\alpha_i$ s'annulent. Seuls les points d'entraînement situés sur la bordure de la marge ou du mauvais côté auront un $\alpha_i > 0$. Ces points critiques sont appelés les \emph{vecteurs de support}. La frontière de décision finale ne dépend plus que d'eux.
    \item \textbf{Dépendance exclusive au produit scalaire :} Les vecteurs de données $\vec{x}$ n'apparaissent dans la fonction à optimiser que sous la forme de produits scalaires $(\vec{x}_i \cdot \vec{x}_j)$, ce qui nous permet d'utiliser l'astuce du noyau ou \emph{kernel trick} en anglais.
\end{enumerate}

\subsubsection*{Le Kernel Trick (Astuce du noyau)}

S'il est trop optimiste de pouvoir tracer une frontière rectiligne pertinente dans l'espace de départ qui est de faible dimension, nous pouvons transformer nos données via une fonction non linéaire $\phi : \mathbb{R}^d \rightarrow \mathcal{F}$, où $\mathcal{F}$ est un espace de bien plus grande dimension. 

Plutôt que de calculer explicitement les lourdes coordonnées de $\phi(\vec{x}_i)$ — ce qui serait coûteux voire impossible si $\mathcal{F}$ est de dimension infinie —, la littérature classique \cite{boser1992kernel} souligne que l'algorithme dual n'a besoin que du produit scalaire dans ce nouvel espace. Il suffit alors de définir une fonction noyau $K$ telle que :
\[
K(\vec{x}_i, \vec{x}_j) \;=\; \phi(\vec{x}_i) \cdot \phi(\vec{x}_j)
\]
On remplace simplement le terme $(\vec{x}_i \cdot \vec{x}_j)$ par $K(\vec{x}_i, \vec{x}_j)$ dans le problème dual. L'algorithme se comporte alors comme si nous avions optimisé un hyperplan dans un espace très complexe, celui où vivent les points $\phi(\vec{x}_i)$, tout en conservant une complexité de calcul parfaitement maîtrisée. C'est précisément cette matrice des similarités $K$ (matrice de Gram) que l'ordinateur quantique va se charger de calculer pour nous dans l'approche hybride.

\subsection{Quantum One-Class Support Vector Machine (Q-OCSVM)}
\label{subsec:q-ocsvm}

Si le clustering variationnel vise à regrouper les données en plusieurs catégories, il est souvent plus naturel, dans le contexte de la fraude bancaire, de ne chercher qu'à modéliser ce qui est « normal ». La fraude étant par nature rare et multiforme, il est difficile d'en faire un cluster à part entière. Le \emph{Quantum One-Class SVM} (Q-OCSVM) répond exactement à cette logique, en adaptant au monde quantique le concept d'estimation de support \cite{scholkopf2001ocsvm} : au lieu de partitionner l'espace, nous cherchons à tracer une frontière la plus ajustée possible autour des transactions légitimes. Le problème d'optimisation primal s'écrit alors :

\begin{equation}
\begin{aligned}
    & \min_{\vec{w}, \rho, \vec{\xi}}
    & & \frac{1}{2} \|\vec{w}\|^2 - \rho + \frac{1}{\nu N} \sum_{i=1}^{N} \xi_i \\
    & \text{sous contraintes}
    & & \vec{w} \cdot \phi(\vec{x}_i) \geq \rho - \xi_i, \quad \forall i \in \{1, \dots, N\} \\
    & & & \xi_i \geq 0, \quad \forall i \in \{1, \dots, N\}
\end{aligned}
\label{eq:ocsvm_primal}
\end{equation}

Ainsi, bien que le SVM classique soit fondamentalement un algorithme d'apprentissage supervisé (nécessitant des étiquettes pour séparer deux classes distinctes), l'OCSVM s'inscrit dans un cadre strictement non supervisé. L'astuce géométrique consiste à fournir uniquement des données normales à l'algorithme (ou à défaut des données passées) et à contraindre l'optimisation pour que tous ces points soient confinés du même côté d'un hyperplan, tandis que l'origine de l'espace de représentation est artificiellement repoussée de l'autre côté, jouant le rôle de la classe adverse.

L'intérêt majeur de cette méthode pour notre pipeline hybride réside dans la séparation très nette des tâches. Nous n'utilisons le processeur quantique que pour une seule chose : évaluer à quel point deux transactions se ressemblent. Toute la partie d'optimisation (le tracé de la frontière) reste confiée à un algorithme classique. 

\subsubsection*{Plongement quantique et matrice de Gram}

Classiquement, les algorithmes comme le SVM utilisent l'astuce du noyau (\emph{kernel trick}) pour projeter les données dans un espace de plus grande dimension (via un noyau gaussien RBF par exemple) afin de faciliter la séparation des points. Dans notre approche quantique, c'est le circuit lui-même qui sert de fonction de projection. 

Concrètement, chaque transaction $\vec{x}_i \in \mathbb{R}^d$ est encodée sous la forme d'un état quantique à l'aide d'un circuit paramétré $U(\vec{x}_i)$. L'état obtenu est :
\[
\ket{\psi(\vec{x}_i)} \;=\; U(\vec{x}_i)\ket{0}^{\otimes n}
\]
L'espace dans lequel nous travaillons n'est plus un espace euclidien standard, mais l'espace de Hilbert de nos $n$ qubits, dont la dimension croît de manière exponentielle en $2^n$. En utilisant des circuits d'encodage qui intriquent les qubits (comme les feature maps basés sur des portes de Pauli \cite{havlicek2019}), nous exploitons des corrélations très complexes entre les différentes caractéristiques (montant, heure, localisation) de la transaction. La similarité entre deux transactions $\vec{x}_i$ et $\vec{x}_j$ correspond alors tout simplement à la probabilité de transition entre leurs deux états quantiques, c'est-à-dire leur fidélité :
\[
K_Q(\vec{x}_i, \vec{x}_j) \;=\; \bigl|\!\braket{\psi(\vec{x}_i)\,|\,\psi(\vec{x}_j)}\!\bigr|^2
\]

\subsubsection*{Le circuit de calcul (Compute-Uncompute)}

Pour estimer cette fidélité sur machine, nous pourrions utiliser le \emph{SWAP test} présenté dans la section précédente. Cependant, le SWAP test nécessite de doubler le nombre de qubits pour stocker les deux états simultanément, en plus d'un qubit ancillaire. Sur les processeurs actuels, chaque qubit supplémentaire apporte du bruit.

D'après la littérature d'apprentissage quantique à base de noyaux \cite{schuld2019qml} il est préférable d'utiliser une technique appelée \emph{inversion} ou \emph{compute-uncompute}. L'idée est de n'utiliser qu'un seul registre de $n$ qubits. On applique d'abord le circuit d'encodage pour la première transaction, puis on applique l'inverse du circuit paramétré avec la seconde transaction :
\[
\ket{\psi_{i,j}} \;=\; U^\dagger(\vec{x}_j) \, U(\vec{x}_i)\ket{0}^{\otimes n}
\]
Si les deux transactions sont identiques ($\vec{x}_i = \vec{x}_j$), l'opération globale équivaut à l'identité, et le registre retourne exactement dans l'état $\ket{0}^{\otimes n}$. Si elles diffèrent, la probabilité de mesurer l'état de base diminue. Ainsi, en mesurant simplement la fréquence d'apparition de la chaîne de bits $00\dots0$, nous obtenons une estimation directe de notre noyau quantique.

\begin{figure}[H]
\centering
\shorthandoff{:}
\begin{quantikz}
\lstick{$\ket{0}^{\otimes n}$} & \gate{U(\vec{x}_i)} & \gate{U^\dagger(\vec{x}_j)} & \meter{}
\end{quantikz}
\shorthandon{:}
\caption{Évaluation de la similarité entre deux transactions par la méthode d'inversion. Le registre est préparé selon les caractéristiques du point $\vec{x}_i$, puis subit la transformation inverse paramétrée par $\vec{x}_j$. La mesure de l'état fondamental $\ket{0\dots0}$ nous donne directement la valeur du noyau $K_Q(\vec{x}_i, \vec{x}_j)$.}
\label{fig:compute-uncompute}
\end{figure}

\subsubsection*{Entraînement et limites de passage à l'échelle}

Une fois que le circuit quantique a calculé les similarités pour toutes les paires de transactions de notre jeu d'entraînement, nous obtenons une matrice dense, la matrice de Gram. Celle-ci est transmise à un optimiseur classique qui va chercher à isoler les données en ajustant un paramètre $\nu$ qui est inversement proportionnel au paramètre de pénalité $C$ indiqué dans l'équation d'optimisation du SVM dans la section précédente (et qui représente concrètement la proportion de faux positifs que la banque est prête à tolérer).

Le principal verrou de cette méthode, que nous avons dû affronter lors des tests sur les données de la BNP, est sa complexité quadratique. Pour $N$ transactions d'entraînement, il faut exécuter $N(N-1)/2$ circuits quantiques, et ce, pour plusieurs milliers de batchs à chaque fois afin d'avoir une estimation statistique fiable. Sur un jeu de données bancaire massif, le calcul complet de la matrice devient vite irréalisable en un temps raisonnable. C'est pourquoi nous avons dû accepter l'idée de ne conserver qu'un sous-échantillon lors des implémentations de nos algorithmes sur le dataset fourni par la BNP. 

\subsubsection*{Algorithme complet}

L'implémentation de notre brique Q-OCSVM suit la séquence classique-quantique suivante :

\begin{enumerate}
    \item \textbf{Préparation classique :} À partir de l'ensemble d'entraînement constitué de transactions supposées normales $\mathcal{X}_{\text{norm}} \subset \mathbb{R}^d$, on sélectionne (par sous-échantillonnage) un sous-ensemble gérable de $N$ transactions.
    \item \textbf{Évaluation quantique (Construction de la matrice de Gram) :}
    \begin{itemize}
        \item Initialiser une matrice $\mathcal{K}$ de taille $N \times N$.
        \item Pour chaque paire de points $(\vec{x}_i, \vec{x}_j)$ avec $i \leq j$ :
        \begin{itemize}
            \item Exécuter le circuit d'inversion $U^\dagger(\vec{x}_j) U(\vec{x}_i)\ket{0}^{\otimes n}$ sur le simulateur.
            \item Estimer la fidélité $K_Q(\vec{x}_i, \vec{x}_j)$ via la fréquence de mesure de l'état $\ket{0}^{\otimes n}$ sur $P$ répétitions.
            \item Remplir la matrice : $\mathcal{K}_{ij} = \mathcal{K}_{ji} = K_Q(\vec{x}_i, \vec{x}_j)$.
        \end{itemize}
    \end{itemize}
    \item \textbf{Optimisation classique :} Fournir la matrice de Gram $\mathcal{K}$ et le paramètre $\nu$ à un solveur SVM classique (par exemple via \texttt{scikit-learn}) pour déterminer les vecteurs de support $\vec{\alpha}$ et le biais $\rho$.
    \item \textbf{Inférence (Nouvelle transaction) :} Pour évaluer une nouvelle transaction $\vec{z}$ :
    \begin{itemize}
        \item Utiliser le circuit quantique pour calculer les noyaux $K_Q(\vec{x}_{\text{sv}}, \vec{z})$ uniquement avec les transactions $\vec{x}_{\text{sv}}$ identifiées comme vecteurs de support lors de l'entraînement.
        \item Calculer classiquement le score de décision $f(\vec{z}) = \sum_{\text{sv}} \alpha_{\text{sv}} K_Q(\vec{x}_{\text{sv}}, \vec{z}) - \rho$.
        \item Classer $\vec{z}$ comme fraude si $f(\vec{z}) < 0$.
    \end{itemize}
\end{enumerate}

\section{Réseaux à de neurones}

Les réseaux de neurones sont un cas particulier de réseaux à couches dans lequel une couche réalise typiquement une transformation affine suivie d’une non-linéarité :
\[
\mathcal{L}(\mathbf{x}) = \phi(W\mathbf{x} + \mathbf{b}),
\]
où $W$ est une matrice de poids, $\mathbf{b}$ un biais, et $\phi$ une fonction d’activation. L’empilement de plusieurs couches permet de construire des représentations hiérarchiques des données, chaque niveau extrayant des caractéristiques plus abstraites que le précédent.
Dans le cadre quantique, comme déjà mentionné dans la section 4, cette idée se transpose sous la forme de circuits paramétrés organisés en blocs successifs. Nous nous interreserons dans la suite aux réseaux de neurones adversariaux, un modèle de réseaux de neurones pertinent pour la détection d'anomalie.

\subsection{Réseaux de neurones adversariaux}

Les réseaux de neurones adversariaux, connus sous le nom de Genarative Adversarial Networks (GANs) en anglais, sont une classe d'algorithmes d'apprentissage non supervisé. Introduis en 2014 par Goodfellow et al. 2014, il s'agit d'un modèle génératif où deux réseaux de neuronnes sont placés en compétition dans un scénario de théori des jeux. L'un génère un échantillion, tandis que son adversaire (discriminateur), essaie de détecter s'il est réel ou pas. En repetant cet exercice, le discriminateur s'entraîne à discriminer le vrai du faux tandis que le générateur, se rapproche de la réalité. Le générateur attribue un score compris entre 0 et 1 : un score de 1 signifie que les données semblent réelles et un score de 0 signifie qu’elles sont fausses.
Le générateur utilise ensuite les commentaires du discriminateur pour s’améliorer, en essayant de créer des données plus réalistes \cite{DanielGANs}. 
Ce modèle a connu de nombreux succès en imagerie et en neurosciences.  

\subsubsection{Théorie}

Mathématiquement parlant, un GAN est basé sur un jeux à somme nulle entre deux joueurs, défini sur un espace de probabilité $ ( \Omega, \mathcal{B}, \mu_{r})$. L'ensemble des stratégies du générateur décrit l'ensemble des mesures de probabilité $ \mu_{G}$ sur $ ( \Omega, \mathcal{B})$ et l'ensemble des stratégies du discriminateur décrit l'ensemble des fonctions mesurables $D : \Omega \rightarrow [0,1]$. Le générateur et le discriminateur visent respectivement à minimiser et à maximiser une fonction de gain, soit le problème minmax: 
$$ \underset{G}{\min}\;\underset{D}{\max}\; L(\mu_{G},D)=
\mathbb{E}_{x \sim \mu_{\mathrm{ref}}}\bigl[\ln D(x)\bigr]
+
\mathbb{E}_{x \sim \mu_G}\bigl[\ln(1 - D(x))\bigr].$$
Un \href{https://fr.wikipedia.org/wiki/R%C3%A9seaux_antagonistes_g%C3%A9n%C3%A9ratifs}{théorème de base du GAN} nous assure de la convergence de l'algorithme en adaptant à chaque étape le discriminateur par: 
$D^*(x) = \frac{d\mu_r}{d(\mu_r + \mu_G)}(x)$. Mais en pretique, on observe des problèmes de convergence et le processus peut s'avérer coûteux en calculs sur des jeux de données de grande dimenssion comme dans notre cas.

Heuresement, des travaux sur des variantes quantiques ont permis de mettre en oeuvre une approche quantique qui améliore considérablement la complexité de calcul \cite{QGANs}. Dans le formalisme quantique, la fonction de s'exprime par:
 \[
\max_{\hat{U}_G} \; \max_{\hat{U}_D} \;
\operatorname{Tr}\!\big(\hat{\rho}_r \hat{U}_D\big)
+
\operatorname{Tr}\!\big(\hat{\rho}_g \hat{U}_G \hat{U}_D\big).
\]
où $\hat{U}_D$ et $\hat{U}_G$ désignent respectivement les opérateurs quantiques correspondant respectivement au discrimineur et au générateur dans l'espace de hilbert $\mathcal{H}$ dans lequel sont encodées les données et $\hat{\rho}_g$ et $\hat{\rho}_r$ font référence aux densités de données réelles et générées.

$\hat{U}_D$ et $\hat{U}_G$ s'expriment en employant des transformations unitaires impliquant leurs hamilitoniens respectifs, augmentés d'un terme supplémentaires: 
\[
\hat{U}_G(t) = e^{-i(\hat{H}_G +\lambda \hat{V_G})t} ,~
\hat{U}_D(t) = e^{-i(\hat{H}_D+\lambda \hat{V_D})t}.
\].

Les paramètres $\hat{V_G}$ et $\hat{V_D}$ permettent d’augmenter l’espace des transformations accessibles, en intégrant des interactions quantiques plus complexes, en particulier de l’intrication ou des corrélations d’ordre supérieur. Le paramètre lambda permet donc de contrôler l'intensité de cette expressivité. 

\subsubsection{QGANs pour la détection de Fraude}

Il convient de préciser que les GANs ont été à l'origine conçus pour des problèmes d'imageries comme la génération d'image, la reconnaissance faciale, la détection d'objets. Ce n'est que longtemps après que le paradigme s'est étendue à la détection de fraude. En pratique, qu'est-ce que ça donne? Inspiré par (Seyyede, Ali \& Saeed, 2023) \cite{GANs}, on introduit un model à trois composantes principales: 

\begin{itemize}
    \item{\textbf{Génération d'anomalies}}: La première composante est un modèle de QGAN, utilisé afin de générer des observations synthétiques atypiques. Ces données artificielles sont conçues pour être difficilement distinguables des données réelles, tout en se situant en périphérie de la distribution, ce qui permet de simuler des comportements potentiellement frauduleux.

    \item{\textbf{Augmentation des données}}: La seconde étape est la création d'un ensemble de données étiqueté standard, réalisée en trois étapes. Dans la première étape, les échantillons aberrants produits par la première composante sont ajoutés à la fin des données réelles. Il convient de noter que le nombre d'échantillons artificiels aberrants est considéré comme égal au nombre d'échantillons réels pour l'équilibre des données. Dans la deuxième étape, tous les échantillons sont étiquetés selon l'une des deux approches possibles : (1) utiliser des algorithmes d'apprentissage automatique pour estimer une étiquette pseudo-cible pour les échantillons, ou (2) attribuer des étiquettes sujettes à la fraude pour les échantillons synthétiques et des étiquettes sans fraude pour les échantillons dans les données principales. Dans la dernière étape, tous les échantillons sont mélangés pour garantir une distribution appropriée des échantillons dans les ensembles de données d'entraînement et de test, ce qui conduit à une meilleure généralisation et à l'évitement du surapprentissage.

    \item{\textbf{Détection de Fraude}} Enfin, une troisième étape consiste à appliquer une méthode de classification sur le dataset synthétisé à l'étape précédente de classification binaire. Ce dernier apprend à distinguer les comportements normaux des anomalies synthétiques, permettant ainsi de détecter des états financiers frauduleux. On fait le choix d'utiliser un model XGBOD pour la classification.
    La figure~\ref{fig:qgan-pipeline} illustre les trois étapes principales du modèle. 
    
\end{itemize}

\begin{figure}[H]
\centering
\resizebox{\textwidth}{!}{
\begin{tikzpicture}[
    scale=0.75,
    transform shape,
    node distance=1.2cm and 1.8cm,
    box/.style={draw, rounded corners, align=center, minimum height=0.9cm, minimum width=2.4cm, font=\small},
    gen/.style={box, fill=blue!10},
    disc/.style={box, fill=red!10},
    proc/.style={box, fill=orange!15},
    model/.style={box, fill=green!15},
    quant/.style={box, fill=purple!10},
    arr/.style={-{Latex[length=2mm]}, thick}
]

% Input
\node (real) {Données réelles $\mathbf{x}$};

% Encodage
\node[quant, right=of real] (encode) {Encodage\\ $|\psi(x)\rangle = U_\Phi(x)|0\rangle$};

% Latent
\node[gen, above=of encode] (z) {Latent $z \sim p(z)$};

% Generateur
\node[quant, right=of encode] (gen) {Générateur\\ $\hat{U}_G$};

% Discriminateur
\node[quant, right=of gen] (disc) {Discriminateur\\ $\hat{U}_D$};

% Post-processing
\node[proc, below=of disc] (concat) {Concaténation};
\node[proc, below=of concat] (label) {Pseudo-labels};
\node[proc, below=of label] (shuffle) {Mélange};

\node[proc, left=of shuffle] (features) {Feature selection};

% Classifieur
\node[model, left=of features] (clf) {Classifieur\\ XGBOD};

\node[model, below=of clf] (output) {Fraude / Non-fraude};

% Arrows
\draw[arr] (real) -- (encode);
\draw[arr] (encode) -- (gen);
\draw[arr] (z) -- (gen);
\draw[arr] (gen) -- (disc);
\draw[arr] (disc) -- (concat);
\draw[arr] (concat) -- (label);
\draw[arr] (label) -- (shuffle);
\draw[arr] (shuffle) -- (features);
\draw[arr] (features) -- (clf);
\draw[arr] (clf) -- (output);

\end{tikzpicture}
}
\caption{Pipeline hybride de détection de fraude avec un QGAN.}
\label{fig:qgan-pipeline}
\end{figure}

\label{sec:cascade}
 
\subsection{Motivation et positionnement dans la pipeline}
 
Les sections précédentes ont présenté deux grandes familles d'algorithmes non supervisés~: le clustering quantique variationnel (VQC, section~\ref{subsec:vqc-clustering}) et le Q-OCSVM (section~\ref{subsec:q-ocsvm}). Ces approches partagent une même logique --- modéliser le comportement normal des transactions sans recourir à des labels --- et constituent le c\oe ur de notre contribution quantique. Leur limite commune est cependant bien identifiée~: en l'absence de labels, le signal de détection reste flou, et le calibrage du seuil de décision ($\tau$ dans le VQC, $\nu$ dans l'OCSVM) repose sur des heuristiques ou sur un sous-ensemble annoté a posteriori.
 
Nous introduisons ici une troisième brique dans la pipeline modulaire~: la \emph{cascade à couches} (\textit{layered cascade}). Son principe est radicalement différent des approches précédentes --- elle exploite les labels d'entraînement disponibles sur les jeux de données publics pour apprendre une frontière de décision progressive --- mais elle s'inscrit dans la même architecture \textit{plug \& play}~: elle respecte la même interface d'entrée/sortie que les briques quantiques et peut s'y substituer ou s'y combiner dans le schéma d'assemblage décrit en section~\ref{sec:pipeline}.
 
La cascade remplit deux rôles complémentaires dans notre démarche. D'abord, elle constitue la \textbf{baseline supervisée classique} à laquelle nous comparons les performances des algorithmes quantiques dans la section~\ref{sec:tests}~: sans cette référence, il serait impossible de mesurer l'apport réel des briques quantiques. Ensuite, et c'est l'objet de la seconde partie de cette section, la cascade est un \textbf{cadre algorithmique extensible}~: nous explorons dans quelle mesure une ou plusieurs de ses couches peuvent être remplacées par des circuits quantiques variationnels, ouvrant la voie à une \emph{cascade hybride} dont nous discutons le potentiel et les limites à la lumière de la littérature récente.
 
\subsection{Principe de la détection en cascade}
 
L'idée directrice est simple~: plutôt que d'appliquer un unique modèle lourd à l'ensemble des transactions, on enchaîne plusieurs classifieurs de complexité croissante, chacun ne traitant que les cas que le modèle précédent a jugés suspects. Cette architecture est motivée par deux contraintes propres à la détection de fraude.
 
La première est le \emph{déséquilibre extrême des classes}~: les fraudes représentent typiquement moins de $0{,}2\,\%$ des transactions dans le jeu European Credit Card que nous utilisons pour le benchmarking. Un modèle unique appliqué à l'intégralité du flux est mécaniquement biaisé vers la classe majoritaire. Les briques de prétraitement (SMOTE, Tomek Links) décrites en section précédente corrigent partiellement ce biais à l'entraînement, mais pas à l'inférence, où le déséquilibre est structurel.
 
La seconde contrainte est le \emph{compromis rappel/précision asymétrique} propre au domaine bancaire~: bloquer la carte d'un client légitime (faux positif) a un coût d'expérience utilisateur mesurable, mais manquer une fraude (faux négatif) a un coût financier et réglementaire direct. La cascade permet de gérer ce compromis de manière granulaire, en ajustant indépendamment le seuil de chaque couche plutôt qu'un unique hyperparamètre global.
 
\paragraph{Architecture générale.} La cascade est organisée en $L$ étages ordonnés du plus simple au plus complexe. Chaque étage $\ell$ reçoit un sous-ensemble $\mathcal{S}_\ell \subseteq \mathcal{X}$ en entrée, produit une probabilité de fraude $p_\ell(\vec{x})$ pour chaque transaction, et transmet à l'étage suivant uniquement celles vérifiant $p_\ell(\vec{x}) > \tau_\ell$, où $\tau_\ell$ est un seuil propre à chaque couche. Les transactions rejetées à l'étage $\ell$ sont définitivement classées comme légitimes. La décision finale est émise par le dernier étage $L$.
 
\begin{figure}[H]
\centering
\begin{tikzpicture}[
    node distance=0.8cm and 1.4cm,
    box/.style={draw, rounded corners, align=center, minimum height=1cm, minimum width=2.6cm, font=\small},
    lbox/.style={box, fill=green!10},
    mbox/.style={box, fill=yellow!15},
    hbox/.style={box, fill=red!10},
    arr/.style={-{Latex[length=2mm]}, thick},
    rejarr/.style={-{Latex[length=2mm]}, thick, dashed, gray}
]
\node[lbox] (l1) {\textbf{Couche 1}\\Régression\\Logistique\\$\tau_1 = 0{,}01$};
\node[mbox, right=of l1] (l2) {\textbf{Couche 2}\\Réseau de\\neurones (MLP)\\$\tau_2 = 0{,}01$};
\node[hbox, right=of l2] (l3) {\textbf{Couche 3}\\XGBoost\\(Expert)\\Décision dure};
\node[above=0.6cm of l1, font=\small\itshape] (allin) {Toutes les transactions};
\node[below=0.6cm of l1, font=\small, text=gray] (rej1) {Légitime (\textit{rejet})};
\node[below=0.6cm of l2, font=\small, text=gray] (rej2) {Légitime (\textit{rejet})};
\node[right=1.1cm of l3, font=\small] (out) {Fraude / Légitime};
\draw[arr] (allin) -- (l1);
\draw[arr] (l1) -- node[above, font=\scriptsize]{suspects} (l2);
\draw[arr] (l2) -- node[above, font=\scriptsize]{suspects} (l3);
\draw[arr] (l3) -- (out);
\draw[rejarr] (l1.south) -- (rej1);
\draw[rejarr] (l2.south) -- (rej2);
\end{tikzpicture}
\caption{Architecture de la cascade classique à trois couches. Les transactions rejetées à chaque étage sont définitivement classées comme légitimes~; seuls les suspects progressent vers le classifieur suivant, plus complexe.}
\label{fig:cascade-archi}
\end{figure}
 
\subsection{Description des trois couches classiques}
 
Nous implémentons une cascade à $L = 3$ étages sur le jeu European Credit Card (285\,315 transactions, 492 fraudes, soit $0{,}172\,\%$). Le prétraitement appliqué en amont est celui décrit en section précédente~: un \texttt{RobustScaler} est appliqué aux colonnes \texttt{Amount} et \texttt{Time} --- le \texttt{RobustScaler} étant préféré au \texttt{StandardScaler} car moins sensible aux montants extrêmes caractéristiques des fraudes --- et le jeu est découpé en 80/20 avec stratification sur les labels.
 
\subsubsection*{Couche 1 : La Régression Logistique (le tamis)}
 
La première couche est intentionnellement le modèle le plus simple et le plus rapide. Son rôle n'est pas de décider avec précision, mais de filtrer massivement les transactions manifestement légitimes tout en garantissant un rappel quasi parfait sur les fraudes. On entraîne une régression logistique avec l'option \texttt{class\_weight=`balanced'}, qui compense le déséquilibre en attribuant un poids inversement proportionnel à la fréquence de chaque classe. Le seuil est fixé à $\tau_1 = 0{,}01$~: toute transaction pour laquelle le modèle estime une probabilité de fraude supérieure à $1\,\%$ est transmise à la couche suivante. Ce seuil volontairement très bas garantit un rappel maximal --- aucune fraude ne doit être perdue dès la première couche --- au prix d'un nombre important de faux positifs transmis en aval. En pratique, cette couche élimine entre 60 et 70\,\% des transactions légitimes dès la première étape.
 
\subsubsection*{Couche 2 : Le Réseau de Neurones MLP (l'analyste)}
 
La deuxième couche est un réseau de neurones multicouche (\textit{Multi-Layer Perceptron}, MLP), entraîné \emph{uniquement} sur les transactions que la régression logistique a jugées suspectes. Cette restriction est cruciale~: en focalisant l'apprentissage sur les cas ambigus, le MLP développe une représentation interne spécifiquement adaptée à la zone de frontière difficile entre faux positifs tenaces et vraies fraudes. L'architecture comporte deux couches cachées de tailles $32$ et $16$ neurones respectivement, avec activation \texttt{ReLU} et optimiseur \texttt{Adam}. La régularisation $L_2$ (paramètre \texttt{alpha}$= 10^{-4}$) prévient le surapprentissage sur le sous-ensemble réduit reçu en entrée. Le seuil est maintenu à $\tau_2 = 0{,}01$.
 
\subsubsection*{Couche 3 : XGBoost (l'expert)}
 
La dernière couche est le modèle le plus puissant de la cascade. Elle ne reçoit qu'une infime fraction des données originales --- les cas que ni la régression logistique ni le MLP n'ont su écarter --- et doit trancher de manière définitive. On utilise un XGBoost avec 200 arbres (\texttt{n\_estimators=200}), un taux d'apprentissage faible (\texttt{learning\_rate=0.05}) et \texttt{scale\_pos\_weight=20} pour compenser le déséquilibre résiduel dans le sous-ensemble. La décision est dure~: le modèle produit un label binaire sans seuil intermédiaire.
 
\begin{table}[H]
\centering
\begin{tabular}{lccc}
\hline
Étage & Modèle & Seuil $\tau$ & Données reçues (estimation) \\
\hline
1 & Régression Logistique & $0{,}01$ & $100\,\%$ \\
2 & MLP ($32 \to 16$ neurones, ReLU) & $0{,}01$ & $\sim 30\text{--}40\,\%$ \\
3 & XGBoost (200 arbres) & décision dure & $< 5\,\%$ \\
\hline
\end{tabular}
\caption{Résumé des trois couches de la cascade classique, avec le seuil de filtrage et le volume de données traité à chaque étage.}
\label{tab:cascade-summary}
\end{table}
 
\subsection{Résultats de la cascade classique}
 
La cascade est évaluée sur le jeu de test (20\,\% du dataset, jamais vu à l'entraînement) via la fonction d'inférence en cascade, qui reproduit fidèlement le filtrage progressif sans aucune fuite de données. Les résultats sont reportés dans le tableau~\ref{tab:cascade-results}.
 
\begin{table}[H]
\centering
\begin{tabular}{lcc}
\hline
Métrique & Cascade 3 couches & Random Forest (\textit{ref.~section~\ref{sec:tests}}) \\
\hline
F1-score (classe fraude) & \textit{voir notebook \texttt{Final.ipynb}} & $0{,}94$ \\
Rappel & $\geq 0{,}90$ (estimé) & $0{,}97$ \\
Précision & améliorée vs.~LogReg seul & $0{,}69$ \\
Transactions vues par couche 3 & $< 5\,\%$ & $100\,\%$ \\
\hline
\end{tabular}
\caption{Résultats de la cascade comparés à la baseline Random Forest de la section~\ref{sec:tests}. Les valeurs exactes sont à compléter à partir du notebook \texttt{Final.ipynb} (graine \texttt{SEED=19}).}
\label{tab:cascade-results}
\end{table}
 
\paragraph{Bilan intermédiaire.} Le premier enseignement est que la cascade atteint de très bonnes performances de rappel tout en maintenant le modèle expert (XGBoost) à un volume de traitement réduit~: plus de $95\,\%$ des transactions sont décidées en une ou deux étapes seulement, ce qui rend l'approche efficace en production. Contrairement au VQC et au Q-OCSVM, la cascade \emph{nécessite} des labels d'entraînement. Elle ne peut donc pas être appliquée directement aux données BNP non labellisées. En revanche, elle offre un point de comparaison rigoureux sur les jeux publics étiquetés et constitue un candidat naturel pour l'étape de classification supervisée dans un schéma hybride.
 
\subsection{Extension quantique de la cascade}
\label{subsec:cascade-quantique}
 
La cascade classique décrite ci-dessus repose sur trois couches entièrement classiques. Une question naturelle se pose alors~: est-il possible, et pertinent, de remplacer une ou plusieurs de ces couches par un circuit quantique variationnel~? Nous explorons ici cette direction à la lumière de la littérature récente, en distinguant les substitutions qui nous semblent justifiées sur le plan algorithmique de celles qui restent aujourd'hui essentiellement prospectives.
 
\subsubsection{Pourquoi insérer une couche quantique dans la cascade~?}
 
L'argument principal en faveur de l'hybridation est celui de l'\emph{expressivité de l'espace de Hilbert}~: en encodant une transaction $\vec{x} \in \mathbb{R}^d$ dans un état quantique $\ket{\psi(\vec{x})} = U(\vec{x})\ket{0}^{\otimes n}$, on plonge les données dans un espace de dimension $2^n$, dans lequel des frontières de décision non linéaires très complexes peuvent être représentées avec un nombre de paramètres bien inférieur à ce qu'exigerait un réseau classique de même expressivité \cite{havlicek2019, schuld2019qml}. Cet avantage est particulièrement pertinent pour les couches intermédiaires de la cascade, qui opèrent sur des sous-ensembles très réduits ($< 5\,\%$ des données) et doivent tracer des frontières fines entre faux positifs tenaces et vraies fraudes --- exactement le type de problème pour lequel l'expressivité du noyau quantique est susceptible d'apporter un gain.
 
Un second argument est celui de la \emph{représentation des dépendances temporelles}. Dans un contexte bancaire réel, les transactions ne sont pas indépendantes~: l'historique récent d'un client est un signal fort de fraude. El Alami et al.~\cite{elalami2026} montrent que des architectures QML variées (VQC, SQNN, EQNN) parviennent à capter des patterns de fraude sur le jeu European Credit Card, avec un F1-score atteignant $0{,}88$ pour la meilleure configuration (VQC avec Z feature map et ansatz Pauli Two Design). Plus directement, Ubale et al.~\cite{qlstm2025} démontrent qu'un LSTM classique couplé à un circuit variationnel (AngleEmbedding + StronglyEntanglingLayers, optimisé par parameter-shift rule) dépasse un LSTM classique de même taille sur des données synthétiques de transactions~: F1-score de $95{,}39\,\%$ contre $94{,}25\,\%$, avec un rappel supérieur ($96{,}67\,\%$ contre $92{,}93\,\%$). Ce dernier point est directement transposable à la couche 2 de notre cascade.
 
\subsubsection{Quelles couches remplacer~?}
 
Nous distinguons trois stratégies de substitution, de la plus partielle à la plus ambitieuse.
 
\paragraph{Stratégie A~: substitution de la couche 2 par un circuit hybride (QLSTM).} La couche 2 est celle dont la tâche est la plus subtile~: elle doit discriminer les vrais suspects des faux positifs de la régression logistique, sur un sous-ensemble réduit. C'est précisément le régime dans lequel un réseau de neurones hybride classique/quantique est susceptible d'apporter un gain. On peut s'inspirer directement de l'architecture QLSTM de Ubale et al.~\cite{qlstm2025}, en chaînant~: (i) un LSTM classique qui extrait des dépendances séquentielles sur une fenêtre glissante de transactions, (ii) une couche pleinement connectée qui projette l'état caché $h_T$ vers $n_{\text{qubits}}$ dimensions, (iii) un circuit variationnel (AngleEmbedding + StronglyEntanglingLayers paramétrées) mesurant dans la base de Pauli-$Z$, et (iv) une couche dense finale de classification. Les paramètres classiques et quantiques sont entraînés conjointement par rétropropagation, les gradients quantiques étant calculés par la \emph{parameter-shift rule}~:
\[
\frac{\partial \mathcal{L}}{\partial \theta_j} \approx \frac{\mathcal{L}(\theta_j + \tfrac{\pi}{2}) - \mathcal{L}(\theta_j - \tfrac{\pi}{2})}{2}.
\]
Cette approche est implémentable dès aujourd'hui avec PennyLane + PyTorch sur \texttt{AerSimulator}, et ne requiert aucun QRAM ni processeur quantique réel. La taille réduite du sous-ensemble transmis à la couche 2 (quelques milliers de transactions au plus) rend le calcul des gradients par parameter-shift envisageable sans surcoût prohibitif~: Ubale et al. rapportent 45--65 secondes par époque sur 10\,000 points avec 10 qubits, ce qui est cohérent avec notre contrainte.
 
\paragraph{Stratégie B~: substitution de la couche 3 par un Q-SVM supervisé.} La couche 3 est celle qui reçoit le sous-ensemble le plus réduit et doit produire la décision finale. C'est donc le contexte le plus favorable à un classifieur à noyau quantique~: la complexité quadratique de la matrice de Gram (déjà identifiée comme le principal verrou du Q-OCSVM en section~\ref{subsec:q-ocsvm}) est atténuée par la petite taille du sous-ensemble. On substitue XGBoost par un Q-SVM supervisé construit sur le même principe \emph{compute-uncompute} que le Q-OCSVM, mais avec deux classes explicitement labellisées. El Alami et al.~\cite{elalami2026} confirment la faisabilité de cette approche sur le jeu European Credit Card~: des classifieurs QML supervisés (VQC, SQNN) atteignent des F1-scores de $0{,}80$ à $0{,}88$ sur ce jeu, ce qui constitue une référence réaliste pour notre couche 3 quantique. L'analyse ANOVA de cet article identifie par ailleurs le \emph{choix de la feature map et de l'ansatz} comme le facteur statistiquement le plus déterminant après le type de modèle (F-value de $10{,}96$ pour l'ansatz, $p < 10^{-22}$)~: une exploration systématique des configurations feature map/ansatz serait donc nécessaire avant de valider une configuration précise pour notre couche 3.
 
\paragraph{Stratégie C~: cascade entièrement quantique.} La stratégie la plus ambitieuse consiste à remplacer toutes les couches par des circuits quantiques. La couche 1 serait un classifieur QML léger (par exemple un VQC à faible profondeur avec Z feature map, qui présente une bonne convergence et une moindre sensibilité au bruit d'après El Alami et al.~\cite{elalami2026}), la couche 2 un QLSTM hybride, et la couche 3 un Q-SVM supervisé. Sur le plan théorique, cette architecture exploiterait l'espace de Hilbert à chaque étage du filtrage et offrirait la chaîne de représentation la plus expressive. Sur le plan pratique, cette stratégie se heurte à trois verrous cumulatifs~: le coût de simulation de trois circuits distincts sur l'intégralité du pipeline, l'absence de QRAM pour les étages traitant de grands volumes (couche 1), et la difficulté d'optimiser conjointement des circuits aux profondeurs et objectifs très différents.
 
\subsubsection{Analyse de faisabilité et limites}
 
Le tableau~\ref{tab:cascade-hybride} synthétise la faisabilité de chaque stratégie au regard des contraintes matérielles identifiées dans notre projet (simulation sur \texttt{AerSimulator}, absence de QRAM, budget de \textit{shots} limité).
 
\begin{table}[H]
\centering
\begin{tabular}{lccc}
\hline
Stratégie & Couche(s) quantique(s) & Faisable auj. & Gain attendu \\
\hline
A --- Couche 2 hybride (QLSTM) & 2 & \checkmark & Rappel $\uparrow$ \\
B --- Couche 3 Q-SVM supervisé & 3 & \checkmark & Précision $\uparrow$ \\
A + B --- Couches 2 et 3 & 2 et 3 & Partiel & Les deux \\
C --- Cascade entièrement quantique & 1, 2 et 3 & \texttimes & Maximal (théorique) \\
\hline
\end{tabular}
\caption{Stratégies d'hybridation de la cascade, avec évaluation de la faisabilité sur simulateur et du gain attendu sur le F1-score.}
\label{tab:cascade-hybride}
\end{table}
 
\paragraph{Limite commune~: la couche 1 doit rester classique.} La régression logistique traitant $100\,\%$ des transactions, un remplacement quantique à cet étage serait rédhibitoire~: la complexité quadratique de la matrice de Gram (pour un Q-SVM) ou le surcoût de simulation d'un VQC (proportionnel au nombre de transactions fois le nombre de \textit{shots}) rendraient le pipeline inutilisable en production. Ce constat est cohérent avec l'observation de Ubale et al.~\cite{qlstm2025}, qui notent que leurs expériences ont été menées sur des sous-ensembles de 10\,000 points --- soit moins de $4\,\%$ de notre jeu d'entraînement complet. La couche 1 classique est donc une contrainte structurelle, non un choix par défaut.
 
\paragraph{Sur les feature maps et ansätze pour les couches 2 et 3.} El Alami et al.~\cite{elalami2026} fournissent un guide empirique précieux~: sur le jeu European Credit Card, la configuration VQC + Z feature map + Pauli Two Design atteint le F1-score le plus élevé ($0{,}88$), avec une précision de $1{,}00$ (aucun faux positif dans le sous-ensemble de test) mais un rappel de $0{,}78$. Pour la couche 3, qui valorise la précision (éviter les blocages de cartes injustifiés), cette combinaison est un candidat naturel. Pour la couche 2, dont on veut maximiser le rappel (ne manquer aucune fraude avant la couche 3), les configurations avec ansatz Two Local ou Efficient SU2 semblent plus appropriées, atteignant des rappels de $0{,}71$ à $0{,}73$ sur le même jeu.
 
\paragraph{Sur la robustesse au bruit.} El Alami et al. évaluent également la robustesse des meilleurs modèles sous cinq types de bruit quantique (amplitude damping, bit flip, dépolarisation, phase damping, phase flip). Le VQC se révèle plus robuste que le SQNN~: son exactitude ne chute pas en dessous de $0{,}55$ même à des niveaux de bruit modérés ($p = 0{,}25$), là où le SQNN stagne entre $0{,}45$ et $0{,}55$ dès le début. En l'absence de QRAM et sur simulateur bruité, le VQC est donc le candidat le plus prudent pour les couches 2 et 3 de la cascade hybride.
 
\subsubsection{Architecture de la cascade hybride proposée}
 
Sur la base de cette analyse, nous proposons l'architecture de cascade hybride suivante, qui constitue une feuille de route pour des travaux futurs ou pour intégration dans la pipeline d'assemblage (section~\ref{sec:pipeline})~:
 
\begin{figure}[H]
\centering
\begin{tikzpicture}[
    node distance=0.8cm and 1.2cm,
    box/.style={draw, rounded corners, align=center, minimum height=1.1cm, minimum width=3cm, font=\small},
    cbox/.style={box, fill=orange!12},
    qbox/.style={box, fill=blue!12},
    arr/.style={-{Latex[length=2mm]}, thick},
    rejarr/.style={-{Latex[length=2mm]}, thick, dashed, gray}
]
\node[cbox] (l1) {\textbf{Couche 1 (classique)}\\LogReg balancée\\$\tau_1 = 0{,}01$ --- \textit{tamis}};
\node[qbox, right=of l1] (l2) {\textbf{Couche 2 (quantique)}\\QLSTM hybride\\(LSTM + VQC)\\$\tau_2$ --- \textit{analyste}};
\node[qbox, right=of l2] (l3) {\textbf{Couche 3 (quantique)}\\Q-SVM supervisé\\(noyau compute-uncompute)\\Décision dure};
\node[above=0.6cm of l1, font=\small\itshape] (allin) {Toutes les transactions};
\node[below=0.6cm of l1, font=\small, text=gray] (rej1) {Légitime};
\node[below=0.6cm of l2, font=\small, text=gray] (rej2) {Légitime};
\node[right=1.0cm of l3, font=\small] (out) {Fraude / Légitime};
\draw[arr] (allin) -- (l1);
\draw[arr] (l1) -- node[above, font=\scriptsize]{suspects} (l2);
\draw[arr] (l2) -- node[above, font=\scriptsize]{suspects} (l3);
\draw[arr] (l3) -- (out);
\draw[rejarr] (l1.south) -- (rej1);
\draw[rejarr] (l2.south) -- (rej2);
\node[draw, dashed, fit=(l2)(l3), inner sep=5pt, label=above:{\footnotesize \emph{Couches quantiques (stratégie A+B)}}] {};
\end{tikzpicture}
\caption{Architecture de cascade hybride proposée (stratégie A+B). La couche 1 reste classique pour des raisons de passage à l'échelle. Les couches 2 et 3 intègrent respectivement un QLSTM hybride et un Q-SVM à noyau quantique, dans la continuité des briques Q-OCSVM développées en section~\ref{subsec:q-ocsvm}.}
\label{fig:cascade-hybride}
\end{figure}
 
Cette architecture présente deux propriétés importantes au regard de notre démarche globale. D'une part, elle \emph{réutilise} directement le circuit \emph{compute-uncompute} développé pour le Q-OCSVM (section~\ref{subsec:q-ocsvm})~: la seule différence entre le Q-OCSVM non supervisé et le Q-SVM supervisé de la couche 3 est que ce dernier dispose de labels pour calibrer la frontière de décision, ce qui améliore mécaniquement la précision. D'autre part, elle \emph{délimite} clairement le périmètre quantique au sous-ensemble le plus difficile à classifier (les cas transmis après deux filtrages successifs), ce qui maximise l'utilité marginale du calcul quantique tout en minimisant son coût global.
 
\subsection{Positionnement dans l'architecture globale}
 
La cascade à couches --- dans sa version classique comme dans sa version hybride --- s'intègre dans notre pipeline modulaire de deux manières complémentaires, illustrées dans la section~\ref{sec:pipeline}.
 
Dans le premier schéma d'assemblage, elle est utilisée de manière \emph{autonome} comme brique supervisée de référence~: elle reçoit les données prétraitées (après SMOTE/Tomek/PCA) et produit directement les décisions de fraude. C'est dans cette configuration qu'elle est évaluée dans le benchmark de la section~\ref{sec:tests}, en tant qu'Algorithme 1 étendu.
 
Dans le second schéma, elle est utilisée en \emph{complément des briques non supervisées}~: le VQC ou le Q-OCSVM fournissent un score d'anomalie $s_i$ pour chaque transaction, et ce score est ajouté comme feature supplémentaire à l'entrée de la cascade. Cette combinaison hybride est analogue à l'Algorithme 3 de la section~\ref{sec:tests} (Q-OCSVM + Random Forest auto-supervisé), mais en remplaçant le Random Forest par notre cascade, qui dispose d'un meilleur contrôle du compromis rappel/précision par étage.
 
Cette double utilisation illustre l'un des principes directeurs de notre démarche~: les briques classiques et quantiques ne sont pas en compétition, mais complémentaires. Les algorithmes quantiques excellent dans la modélisation non supervisée de la normalité (en l'absence de labels)~; la cascade excelle dans l'exploitation de labels existants pour une décision précise et scalable. L'extension hybride de la cascade cherche à faire coopérer ces deux logiques au sein d'un même pipeline, en réservant le calcul quantique aux étages où son expressivité est la plus utile et son coût le plus supportable.

\section{Assemblage d'une pipeline}

\subsection{Architecture modulaire}

\subsection{Résultats}

\section{Tests et Comparaisons}

Comment évaluer notre modèle ? Dans cette partie nous testons notre pipeline en testant 4 modèles différents. Dans un second temps nous évaluons le troisième algorithme, celui de notre pipeline sur le jeu de données de la BNP. 

\subsection{Comparaison de 4 algorithmes sur un dataset tiers}

Nous comparons 4 algorithmes différents qui évaluent nos pistes théoriques de modèle quantique. 

\begin{enumerate}
    \item Algorithme classique supervisé : Preprocessing + Random Forest
    \item Algorithme quantique non supervisé : Q-OCSVM
    \item Algorithme hybride autosupervisé : Q-OCSVM + Random Forest 
    \item Algorithme quantique supervisé : Preprocessing + Q-SVM
\end{enumerate}



Nous évaluons nos quatre algorithmes sur le jeu de données breast cancer \cite{street1993nuclear}, qui regroupe un total de 569 échantillons. Contrairement aux expérimentations portant sur la fraude bancaire, nous avons ici choisi un jeu de données traite une problématique médicale où les étiquettes correspondent aux classes diagnostiques du dataset : on dénombre 357 échantillons de label négatif (maladie bénigne) et 212 échantillons en de label positif (maladie maligne). Nous l'avons choisi car il est de petite taille avec un bon équilibre des classes ce qui permet d'obtenir de bons résultats sans beaucoup de puissance de calcul. L'intégralité du jeu de données a été conservée sans recours au sous-échantillonnage. Les données ont été partitionnées selon un ratio 70/30, délimitant ainsi les ensembles d'entraînement et de test, ce dernier étant ensuite subdivisé pour fournir les points de validation et d'évaluation finale. Afin de projeter les caractéristiques de ce dataset dans un espace d'embedding de 4 dimensions, une pipeline de prétraitement standardisée a été appliquée : elle comprend une imputation par la médiane, une standardisation des variables numériques et une réduction de dimension via PCA. Pour les modèles quantiques, ces coordonnées sont remises à l'échelle dans l'intervalle $[0, \pi]$ pour permettre le calcul du noyau quantique. Enfin, bien que le déséquilibre initial soit modéré, les branches supervisées appliquent un traitement local par SMOTE-Tomek sur les données d'entraînement afin d'alimenter les modèles avec des classes strictement paritaires.






\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{.png/Scores.png} % Vous pouvez ajuster 0.8 (80% de la largeur)
    \caption{Scores des 4 algorithmes sur le jeu de données breast cancer }
    \label{fig:nom_de_votre_image}
\end{figure}

Le tableau de scores révèle que l'approche classique (Random Forest supervisé) domine nettement le classement avec un score F1 de 0,94 et un coefficient de corrélation de Matthews (MCC) de 0,85. L'algorithme hybride (Algo 3) offre un compromis intéressant en parvenant à égaler l'excellent rappel du modèle classique (0,97), bien que cela s'accompagne d'une chute notable de sa précision (0,69). Enfin, si les approches purement quantiques (Q-SVM supervisé et One-Class Q-SVM) demeurent encore en retrait avec les performances globales les plus faibles du comparatif, ces résultats n'en restent pas moins encourageants ; ils démontrent de manière tangible que, malgré les limitations matérielles actuelles, les algorithmes quantiques parviennent effectivement à extraire du signal et à apprendre des représentations pertinentes, ouvrant la voie à de futures optimisations. Bien qu'appliqué à des données bien différentes de celles du rogue trading, l'approche auto-supervisé de l'algorithme 3 donne des résultats très encourageants alors même qu'il est forcément pénalisé vu qu'il n'a pas accès aux labels.  

\subsection{Application au jeu de données de la BNP}

Pour évaluer notre pipeline directement sur le jeu de données de la BNP nous ne disposons malheuresemnt d'aucun label. Ainsi nous avons opté sur deux approches. Premièrement nous pouvons calculer le coefficient de silhouette de notre résultat. Ce coefficient est très répandu dans la littérature pour l'évaluation d'agorithmes non supervisés. 

Le coefficient de silhouette est la moyenne des scores $s_i$ calculés en chaque point. On définit la séparation externe $b_i$ qui est la distance moyenne entre le point $i$ et tous les points du cluster voisin le plus proche ainsi que la cohésion interne $a_i$ qui est la distance moyenne entre le point i et tous les autres points situés dans le même cluster. 

$$s_i = \frac{b_i - a_i}{\max(a_i, b_i)}$$

A noter que chaque $s_i$ et donc le coefficient de silhouette sont dans l'intervalle $[0, 1]$. Le coefficient de silhouette est une métrique de validation qui permet de mesurer la qualité d’une partition de données en évaluant la cohérence de chaque point par rapport à son propre groupe (le cluster) face aux autres. Voir \cite{Rousseeuw1987} et \cite{Shahapure2020} pour plus de précisions sur le coefficient de silhouette et sa pertinence. 

En prennant un sous echantillon de 5000 lignes du jeu de données de la BNP en en appliquant l'algorithme 3 nous obtenons un coefficient de silhouette de $0.046233$. L'algorithme a classé $150$ données comme étant suspectes $(3\%)$ . On remarque que le score de silhouette est bas mais c'était attendu parce que cette métrique favorise des groupes compacts et bien séparés dans l’espace géométrique, alors qu’en fraude les comportements suspects sont souvent diffus, hétérogènes et très minoritaires, donc naturellement proches du nuage principal. La création de cluster n'est pas la philosophie des algorithmes SVM, ce à quoi s'ajoute le faible taux de suspects à $3\%$ ce qui n'optimise pas notre algorithme pour cette métrique. Ainsi la petitesse du coefficient de silhouette n'est pas alarmante et ne remet pas en question le modèle.  


Etant donné la faible validation apportée par le calcul du coefficient de silhouette nous avons décidé de regarder en détail ce que notre modèle nous indiquait comme trade jugé frauduleux. Nous avons pris au hasard 50 données indiquées comme potentiellement frauduleuses par notre algorithme. Nous avons inspecté leurs features et il est aisé d'observer que cette échantillon est anormal dans une certaine mesure. Voici quelques exemples qui étaillent notre propos :

\section{Difficultés et critique}

\subsection{Limites matérielles}

\subsection{Absence de labels dans le jeu de données de la BNP}

L'un des buts du projet fut de pouvoir évaluer notre modèle directement sur le jeu de données de la BNP. D'autant qu'un effort a été porté tout le long de l'année pour respecter les contraintes spécifiques de ce dernier. Comme expliqué dans l'introduction le jeu de données de la BNP qui a été fourni à notre groupe de PSC n'est pas labellisé. Cela pose bien sûr un problème pour l'entrainement d'un modèle car beaucoup sont basés sur l'apprentissage statistique des labels. Nous avons vu qu'il était possible de contourner cet obstacle par l'utilisation de modèles auto-supervisés c'est à dire en créant eux mêmes des "faux" labels pour faire de l'apprentissage sur ces derniers par la suite. De plus les labels servent également à évaluer un modèle. Ainsi un vrai casse tête fut de savoir comment évaluer la performance de nos modèles sur le jeu de données de la BNP. Nous avons finalement opter pour une double approche : calculer le coefficient de silhouette et analyser à la main les données catégorisées comme frauduleuses.

\section{Conclusion et idées futures}

\subsection{Bilan}

\subsection{Idées futures}

\begin{thebibliography}{99}

\bibitem{SMOTE2025}
    J. Thompson et al. 2025. A Quantum Approach to Synthetic Minority Oversampling Technique (SMOTE). Quantum Machine Intelligence.

\bibitem{chawla2002smote} N.~V. Chawla, K.~W. Bowyer, L.~O. Hall, W.~P. Kegelmeyer, \textit{SMOTE: Synthetic Minority Over-sampling Technique}, Journal of Artificial Intelligence Research, vol.~16, pp.~321--357, 2002.

\bibitem{qml_survey} J.~Biamonte, P.~Wittek, N.~Pancotti, P.~Rebentrost, N.~Wiebe, S.~Lloyd, \textit{Quantum Machine Learning}, Nature, vol.~549, pp.~195--202, 2017.

\bibitem{kerenidis_qmeans} I.~Kerenidis, J.~Landman, A.~Luongo, A.~Prakash, \textit{q-means: A quantum algorithm for unsupervised machine learning}, Advances in Neural Information Processing Systems (NeurIPS), 2019.

\bibitem{kerenidis_spectral} I.~Kerenidis, J.~Landman, \textit{Quantum spectral clustering}, Physical Review A, vol.~103, 042415, 2021.

\bibitem{poggiali_kmeans} A.~Poggiali, A.~Berti, A.~Bernasconi, G.~M. Del~Corso, R.~Guidotti, \textit{Quantum Clustering with k-Means: a Hybrid Approach}, Theoretical Computer Science, 2024.

\bibitem{bermejo_orus} P.~Bermejo, R.~Orús, \textit{Variational quantum and quantum-inspired clustering}, Scientific Reports, vol.~13, 13284, 2023.

\bibitem{guo_anomaly} M.~Guo, H.~Liu, Y.~Li, W.~Li, F.~Gao, S.~Qin, Q.~Wen, \textit{Quantum algorithms for anomaly detection using amplitude estimation}, Physica A, vol.~604, 127936, 2022.

\bibitem{liang_anomaly} J.-M. Liang, S.-Q. Shen, M.~Li, L.~Li, \textit{Quantum anomaly detection with density estimation and multivariate Gaussian distribution}, Physical Review A, vol.~99, 052310, 2019.

\bibitem{havlicek2019} V.~Havlíček, A.~D. Córcoles, K.~Temme, A.~W. Harrow, A.~Kandala, J.~M. Chow, J.~M. Gambetta, \textit{Supervised learning with quantum-enhanced feature spaces}, Nature, vol.~567, pp.~209--212, 2019.

\bibitem{elalami2026} M.~El~Alami, N.~Innan, M.~Shafique, M.~Bennai, \textit{Comparative Performance Analysis of Quantum Machine Learning Architectures for Credit Card Fraud Detection}, arXiv:2412.19441, janvier 2026.

\bibitem{circuithunt2025} N.~Innan, A.~Singh, M.~Shafique, \textit{CircuitHunt: Automated Quantum Circuit Screening for Superior Credit-Card Fraud Detection}, arXiv:2508.21366, août 2025.

\bibitem{fidqae2025} M.~El~Alami, A.~Innan, N.~Innan, M.~Shafique, M.~Bennai, \textit{FiD-QAE: A Fidelity-Driven Quantum Autoencoder for Credit Card Fraud Detection}, arXiv:2512.12689, décembre 2025.

\bibitem{qlstm2025} R.~Ubale, S.~K.~K., S.~Deshpande, G.~T. Byrd, \textit{Toward Practical Quantum Machine Learning: A Novel Hybrid Quantum LSTM for Fraud Detection}, arXiv:2505.00137, avril 2025.

\bibitem{abbou2025vqc} F.~M. Abbou, M.~Bouhadda, L.~Bouanane, M.~Kettani, F.~Abdi, A.~Abid, \textit{Comprehensive Analysis of VQC for Financial Fraud Detection: A Comparative Study of Quantum Encoding Techniques and Architectural Optimizations}, arXiv:2509.25245, septembre 2025.

\bibitem{revolut_kaggle} \textit{Revolut Bank Transactions Fraud Detection Dataset}, jeu de données public, Kaggle. \texttt{https://www.archambault.ca/livres/5-ans-de-r}%C3%A9flexions-journal-%C3%A0-compl%C3%A9ter-nul-besoin-d'%C3%AAtre-%C3%A9crivain-pour-faire-de-sa-vie-un-roman-n-%C3%A9d/collectif/9782501097840/?id=1641238}.


\bibitem{street1993nuclear} 
W.~N. Street, W.~H. Wolberg et O.~L. Mangasarian, \textit{Nuclear feature extraction for breast cancer diagnosis}, Biomedical image processing and biomedical visualization, vol. 1905, pp. 132--144, 1993.

\bibitem{cortes1995svm} C.~Cortes, V.~Vapnik, \textit{Support-vector networks}, Machine Learning, vol.~20, pp.~273--297, 1995.

\bibitem{boser1992kernel} B.~E.~Boser, I.~M.~Guyon, V.~N.~Vapnik, \textit{A training algorithm for optimal margin classifiers}, Proceedings of the fifth annual workshop on Computational learning theory, pp.~144--152, 1992.

\bibitem{scholkopf2001ocsvm} B.~Schölkopf, J.~C.~Platt, J.~Shawe-Taylor, A.~J.~Smola, R.~C.~Williamson, \textit{Estimating the Support of a High-Dimensional Distribution}, Neural Computation, vol.~13, pp.~1443--1471, 2001.

\bibitem{schuld2019qml} M.~Schuld, N.~Killoran, \textit{Quantum Machine Learning in Feature Spaces}, Physical Review Letters, vol.~122, 040504, 2019.

\bibitem{Singh2025} Navneet Singh and Shiva Raj Pokhrel, IEEE, Senior Member \textit{Modeling Feature Maps for Quantum Machine
Learning}, arXiv:2501.08205v1 août 2025.

\bibitem{Schuld2020} M.~Schuld, A.~Bocharov, K.~M.~Svore, and N.~Wiebe, \textit{Circuit-centric quantum classifiers}, Physical Review A, vol. 101, no. 3, p. 032308,
2020.

\bibitem{DanielGANs} Daniel Herr et al 2021 Quantum Sci. Technol. 6 045004

\bibitem{GANs} Seyyede~Zahra~Aftabi, Ali~Ahmadi, Saeed~Farzi, \textit{Fraud detection in financial statements using data mining and GAN models}, Expert Systems With Applications 227 (2023) 120144.

\bibitem{QGANs} "*\textit{Quantum Generative Adversarial Networks: Bridging Classical and Quantum Realms}, arXiv:2312.09939v1 [quant-ph] 15 Dec 2023
\end{thebibliography}

\bibitem{Rousseeuw1987} 
P.~J.~Rousseeuw, \textit{Silhouettes: a Graphical Aid to the Interpretation and Validation of Cluster Analysis}, Journal of Computational and Applied Mathematics, vol.~20, pp.~53--65, 1987. \url{https://doi.org/10.1016/0377-0427(87)90125-7}.

\bibitem{Shahapure2020} 
K.~R.~Shahapure, C.~Nicholas, \textit{Cluster Quality Analysis Using Silhouette Score}, IEEE 7th International Conference on Data Science and Advanced Analytics (DSAA), pp.~747--748, 2020. \url{https://doi.org/10.1109/DSAA49011.2020.00096}.

\end{thebibliography}

\end{document}
